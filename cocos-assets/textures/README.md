# L3-b texture export

Ardot scan completed against file `721098916642284`.

No scanned node is owned by an L3-b Frame. `ARCHITECTURE.md` explicitly states that 3D battle, HUD, health bars, and effects were not separately represented in the design file. The scanned image nodes (`Rectangle 1`, game covers, avatars, and multi-window previews) are either unconfirmed or owned by L1, so they are intentionally not exported into the Cocos runtime.

This directory remains empty until a Cocos-owned Frame or a confirmed `Rectangle 1` usage is supplied.
