# 多开 + 同步时「画面元素不全」——Xcode 调试日志分析

**分析对象**：`/Users/gg/log.txt`（1415 行，2026-09-19 22:51）
**场景**：macOS 游戏大厅，7 个实例（21电阴 / 22红血 / 23甜阴 / 24潮阴 / 25破阴 / 26混阴 / 27黑阴），目标帧率 60，同步功能已启用的时段
**症状**：点击窗口后，部分窗口有概率出现画面元素缺失

---

## 一、结论先行

1. **这份日志看不出根因。** 它既没有渲染/GL 层报错，也没有任何"缺块"记录；但它能**排除一大片**嫌疑，并且给出了一个可用的**"点击指纹"**。
2. **日志里"该报的都没报"，不是日志通道坏了** —— 图形通道是活的（能看到 `[instance] WebGL webgl-context-recovery-installed`），同类事件的其它分支完全是静默的。
3. **日志有三个可见性缺口**，不补上就永远无法定位：
   - 帧率下发**不带实例身份**（7 连发里分不清哪条属于哪个账号）；
   - **同步链路 0 条 `[sync]` 埋点** —— "同步是不是诱因"完全没被观测到；
   - 渲染自检**只覆盖 Sprite 贴图**，FGUI / Spine / Label 元素缺失一条都不会报。

---

## 二、日志覆盖了什么

- 起点不是真正的进程首行：**缺 `[lobby] build <tag>`**（启动首行），第一条是 `[instance] restoring persisted game settings`。
  说明这份文本是被过滤过、或粘贴起点靠后的 —— 这点很重要（见 §5）。
- 时间跨度约 1 分钟：`login-stats` 每实例打出 `+10s / +25s / +40s`，最后几行是 `+40s`。
- 规模：7 个 WebContent 进程（PID 71112–71118）、7 次 `authentication started/complete`、7 次 `[instance] ready`（elapsed=402–653ms，全部 `stable=yes`）、CDN `active session count=1→7`。

**这一档一切都正常**：登录、选服、资源清单、CDN 会话、实例就绪、脚本注入（7 个用户脚本 × 7 实例）全部按预期跑完。

---

## 三、日志里**有**的东西

### 3.1 "点击指纹"：`frame rate apply` 的 7 连发

每次点击卡片 → `MatrixStageView.swift:711` `session.focus(account.id)` → `LobbySessionModel.swift:929-933` `reapplyEnergyPolicy()` → **对 7 个实例逐个** `applyEnergyPolicy` → `GameViewportInstance.swift:560` `applyFrameRate`。

三段完整的扇出（每段正好 7 条 = 1 个焦点窗 + 6 个非焦点窗）：

| 日志行 | 内容 | 解读 |
|---|---|---|
| 1260–1266 | `noop:15`×6 + `noop:60` | 点击 ①：全量重下发，但 7 个实例的档位都已正确 → 空转 |
| 1284–1301 | `noop:15`×5 + `ok-restart:15` + `ok-restart:60` + `noop:15` | 点击 ②：**真换焦点** → 旧焦点 60→15、新焦点 15→60，**两条主循环都被拆了重建** |
| 1383–1389 | `noop:60`×1 + `noop:15`×6 | 点击 ③：空转 |

全日志共 **77 条下发**：`deferred:15`×25、`noop:15`×30、`noop:60`×7、`ok-restart:15`×12、`ok-restart:60`×3。

→ **`ok-restart` 共 15 次 = 15 次"主循环拆建"**，全部由点击/焦点切换触发。

### 3.2 已有的渲染自愈通道是活的

`[lobby-macos] [instance] WebGL webgl-context-recovery-installed: WebGL context recovery installed`（行 122/123/389/594/835 等，共 7 条，每实例一条）
→ 证明 `postWebGraphicsLog`（`ios2-web-boot.js:1518`）→ 页面事件 `.graphics` → `GameViewportInstance.swift:826` 这条通道在本次运行中**确实在工作**。

### 3.3 其它正常项

- CDN 下载合并生效：`yuling_1401` 的 4 个 URL 每个只发一次网络请求（行 1348–1380），符合 `CDNAssetStore` actor 的设计。
- 无 `[cdn] known missing`、无 `404 burst`、无 `core prefetch failed`。
- 仅 1 条 `[instance] slow main work: script message 'ready' took 118 ms`（118ms，不至于）。

---

## 四、日志里**没有**的东西（关键）

以下每一项都是"一旦发生必然落日志"的行，全部 0 条：

| 应该出现的日志行 | 触发点（代码位置） | 本次 |
|---|---|---|
| `[instance] render integrity degraded (…): missing=N/M texture=x material=y` | 页面自检 → `.render` → `GameViewportInstance.swift:1047` | **0** |
| `[instance] render integrity recovered after N degraded sample(s)` | 同上 `:1036` | **0** |
| `[instance] WebGL webgl-context-lost: …` / `webgl-context-restored` / `texture-upload-failed` / `pvr-recovery-*` | `ios2-web-boot.js:2032-2077` → `.graphics` → `:827` | **0** |
| `[instance] WebGL context unrecoverable, reloading` | `.webGLFatal` → `:822` | **0** |
| `[instance] auto reloading (n/2): …` | `:1065` | **0** |
| `[instance] JS error: …` | `.error` → `:805` | **0** |
| `[instance] frame rate apply failed: …` | `:575` | **0** |
| `page event: render` 之外的页面事件 | `.unknown` → `:886` | 只有 7 条 `page event: capabilities` |
| **`[sync] …`（publish / ack / drop / page / 主控 / 参与同步）** | `InputSyncController.swift:71/79/405/416/427/586/610/695/709/715` | **0** |

### "没有报"能不能当证据？

**渲染自检这一条能。** `installRenderIntegrityWatchdog`（`ios2-web-boot.js:1975`）在 `sendReady` 时挂起（`:763`），首次采样在 +4s，此后每 12s 一次（`:1978`）。日志里每个实例的 ready 之后都还有 ≥40s 的覆盖（最后几行是各自的 `login-stats +40s`），即**每实例至少 3 次采样**。而自检**只在 `missing > 0` 时才上报**（`:1943-1944`），原生侧**只在 `missing > 0` 时才落日志**（`:1034-1047`）。

→ **在这段日志覆盖的时间窗内，7 个实例在 ≥3 个采样点上都检测到"0 缺块"，也从未发生 WebGL 上下文丢失。**

### 但自检有盲区，别过度解读

`collectRenderIntegrity()`（`ios2-web-boot.js:1876-1908`）的分母只统计**同时满足**：节点 activeInHierarchy、opacity>0、**挂了带 `spriteFrame` 的组件**、且 `texture.loaded === false`。

这个游戏的 UI 是 **FGUI + Spine + Label**（帧率/面板/千机变等都是 FGUI），这些元素**根本不产生 `spriteFrame` 组件**。所以：

> 自检静默 = 只排除了「Sprite 贴图没 load 上来」这一类。
> 如果用户看到缺的是**文字 / 图标 / 骨骼特效 / FGUI 图元**，这套自检一条都不会报。

---

## 五、为什么这份日志答不了这个问题（三个缺口）

### 缺口 1：帧率下发不带实例身份
`GameViewportInstance.swift:573` 只打 `[instance] frame rate apply: <结果>`。7 连发里**无法判断哪条属于哪个账号**，因而**无法把"某个窗口元素不全"与"它的主循环刚被拆建过"对上**。这是本次分析最大的障碍。

### 缺口 2：同步链路完全不可见
按设计，同步链路每一次点击都会留三类签名（`InputSyncController.swift`）：
`[sync] publish A mousedown → 6 目标 [B,C,…]`（:715）、`[sync] ack B mousedown → 回放=n miss=0 落点=CANVAS`（:416）、以及被丢弃的原因（:695/:709）。
**这份日志一条都没有。** 要么当时同步没真正开启，要么文本被过滤/截断。**在拿到这些行之前，"同步是不是诱因"只能算未验证假设。**

### 缺口 3：页面 console 在游戏 boot 后失效
`BootstrapScriptBuilder.swift:517` 把 `console.log` 映射到 `debug` 级 → `[js]` 行；但同文件 `:90-91` 已注明"页面 boot 之后游戏会把 console 整个换掉，我们包装的那层随之失效"。证据：整份日志里 **`[ios2-web] …` 一行都没有**（而这套代码里 `console.log('[ios2-web] …')` 至少几十处，包括 `[ios2-web] startup settled`、`PVR texture loaded` 等）。
→ **页面侧的 `console.warn` 不能作为判据**，只有 `PageEvent`（messageHandler 直发）才可靠。幸运的是上面 §4 表格里的渲染/图形判据**都是走 messageHandler** 的，所以结论仍然成立。

---

## 六、机制候选（按"有日志指纹"排序）

### A. 焦点切换导致的"7 实例同时拆建主循环"（有指纹，优先验证）
`GameViewportInstance.swift:531-557` 的下发脚本：`pause()` → 等 `max(120, 3×帧长)` ms → 改 `config.frameRate` → `_setAnimFrame()` → `resume()`。

引擎侧（`ios-cocos/cocos-project/src/ios2-web-cocos2d.js`）：
- `_setAnimFrame()`（约 632.6k 偏移）会**按档位替换全局** `window.requestAnimFrame` / `window.cancelAnimFrame`：60/30 档用真 rAF，**其它档（含 15）用 `_stTimeWithRAF` = setTimeout + rAF 两层**；同时改写 `cc.director._maxParticleDeltaTime = frameTime × 2`。
- `_stTimeWithRAF` 返回的是 **setTimeout id**，真实 rAF 回调一旦已排队，`clearTimeout` 取消不掉；`_runMainLoop` 的 tick 有 `if (!e._paused)` 闸门兜住，所以**不会双循环**，但"驱动源在 rAF 与 setTimeout 之间反复切换 + 每帧 deltaTime 上限被改写"是结构上的脆弱点。
- 本日志 60↔15 反复切换（15 次 ok-restart），每次点击最多同时动 7 个页面的主循环；7 个 WebGL 上下文共享同一个 GPU 进程。

**为什么与症状吻合**：症状是"点击后 *有概率*" —— 典型的竞态/资源竞争形态，而不是确定性逻辑错误。

### B. 同步链路本身（无指纹，需反证）
开启同步后每个窗口 `capturing=true`：`window` 捕获阶段常驻 7 个监听（`InputSyncController.swift:227-235`）；`blur` 时会本地补发一条 `mouseup`（`releaseLocal`，:220-225）；`mousedown` 时会在页面 `body` 下 append 一个**全屏波纹层**（`position:fixed; 100%×100%; z-index:2147483647; transition` ，:238-262）。
一次点击 = 宿主向 6 个窗口各发 1 次跨进程 `evaluateJavaScript` + 6 次合成 `MouseEvent` 派发 + 1 个带动画的 DOM 层增删。
**注意**：v1 的 `blur` 补发走 `post()` 外发命中过别人 mouseup 的 bug 已在 v2 修掉（`agentVersion = 2`，:98）。本日志无法确认页面跑的是 v2（`[sync] agent … → v2` 这行也没有）。

### C. 已被日志反证
- WebGL 上下文丢失 / 纹理上传失败 / PVR 恢复：0 条。
- CDN 资源缺失、404 风暴、预取失败：0 条。

### D. 环境噪音（排除，但有一个值得确认的点）
日志里大量重复的：
`WebContent[pid] Connection to 'pboard' … Sandbox restriction`、`Failed to set up CFPasteboardRef`、`checkinWithServer Failed bootstrap_lookup2 … coreservicesd`、`CONNECT: … launchservicesd prohibited`、`AudioComponentPluginMgr … Operation not permitted`、`ProcessAssertion::acquireSync Failed to acquire RBS assertion 'WebKit Media Playback'`（PID 71112–71118 + 70701）。
这些都是**未签名 / 缺 entitlement** 的表现（仓库里 `LobbyCoreSystem` 下**没有 `.entitlements` 文件**），与渲染无关。
**但值得确认一件事**：这个问题在 **Release 签名包**上能不能复现？如果只在 Xcode Debug/未签名包上出现，那 WebKit 拿不到媒体/运行断言导致的行为降级就该被列为嫌疑，而不是架构问题。

---

## 七、建议的取证方案（按投入产出排序）

1. **【已做】给帧率日志加身份**（`buildTag` → `2026-09-19.32`）
   `GameViewportInstance.swift:524/560-586`：`applyEnergyPolicy` 把 `isFocused` 透传给 `applyFrameRate`，
   结果行与失败行都带上账号与焦点态。新格式：

   ```
   [instance] frame rate apply: 21电阴.bin 焦点=on  → ok-restart:60
   [instance] frame rate apply: 22红血.bin 焦点=off → noop:15
   [instance] frame rate apply failed: 23甜阴.bin 焦点=off 目标=15: <error>
   ```

   抓取时按账号 grep 即可还原"这个窗口在出事前后被拆过几次主循环"：
   `grep '21电阴' | grep 'frame rate apply'`。
   ⚠️ 记一次 `ok-restart` = 一次主循环拆建；`noop` = 空转，无害。

2. **【判决性证据】复现瞬间手动触发自检**
   在出现缺块的那个窗口控制台执行：
   `window.__ios2RenderIntegrityCheck()`（入口已存在，`ios2-web-boot.js:1992`）
   → 会强制上报 `render` 事件 → 日志出现 `[instance] render integrity degraded (manual): missing=N/M texture=x material=y`，`samples` 字段**直接列出缺的贴图名**。
   - 报 `missing > 0` → 就是"贴图没 load 上来"，接着查 CDN/解析/上传链路；
   - 报 `missing = 0` 而画面确实缺 → 落在自检盲区（FGUI/Spine/Label），需要扩自检覆盖面。

3. **【对照实验矩阵】**每次只改一个变量：
   | 实验 | 预期判据 |
   |---|---|
   | 关掉同步，重复同样的点击序列 | 还缺 → 与同步无关（指向 A）；不缺 → 同步链路是诱因（B） |
   | 开同步但关"波纹特效" | 还缺 → 排除波纹 DOM 层；不缺 → 该层是诱因 |
   | 临时把目标帧率设为 30（或让非焦点也保持同档，不再 60↔15 切换） | 还缺 → 排除主循环重启（A）；不缺 → 坐实 A |
   | 只开 1 个实例重复点击 | 复现不了 → 确认是多开相关的 GPU/合成资源竞争 |
   | Release 签名包跑一遍 | 只在 Debug 复现 → 指向 entitlement 降级（D） |

4. **【下次抓日志的要求】**
   - 要**完整、无过滤**的输出，从 `[lobby] build <tag>` 第一行开始（这次缺首行，说明有截断/过滤）；
   - 报问题前先确认 `[sync] publish / ack` 有出现（否则同步这条线无法排除）；
   - 落盘的 `~/Library/Application Support/GameLobby/diagnostics.log` 只记登录链路，**不含渲染**，别指望它。

---

## 附：本次分析的事实清单（可直接复核）

- 日志规模：1415 行；7 个实例；WebContent PID 71112–71118；另外 70701 反复出现（疑似主进程或 GPU 进程）。
- 日志标签分布：`hsdk`×463 / `scheme`×335 / `instance`×197 / `js`×42 / `login-diag`×28 / `login-stats`×20 / `cdn`×19 / `pool`×14 / `enhance`×14 / `login-proxy`×7。
- `[enhance]` 14 条 = 7 实例 × 2 次（`waiting-require` 一次、`require-threw` 一次）；`hook=0 panel=0`，`uiNote=ui-running sched=1 wrap=1`（UI 加速已挂）。与渲染缺失无关，但说明增强面板/战斗钩子当时都还没装上。
- 窗口布局：所有游戏 WebView 都在**同一个窗口**的 SwiftUI 网格里（`MatrixStageView.swift:706-715`），不是每个实例一个 NSWindow —— 点击只改焦点态与阴影半径，**不改变卡片尺寸**，所以不是"窗口 resize 导致画布重算"那一类问题。

---

# 追加：第二次实测（buildTag .32）——25破阴 启动期"先缺后全"

**现场**：`/Users/gg/log-2314.txt`（用户 23:14 粘贴的那份，已落盘便于复核）
**症状**：25破阴.bin 的画面"突然没有加载完成，然后全部显示全了"
**这一版的新变化**：目标帧率 = **30**（不是上一份的 60）；帧率日志已带账号身份。

## 9.1 先说新日志怎么读（一个坑）

`frame rate apply` 的日志行是 **JS 返回时**才打印的，不是下发时：

- `deferred` / `noop` 走同步返回 → 日志顺序 ≈ 下发顺序；
- **`ok-restart` 要先 `await` 120ms 以上才返回** → 它的日志会晚 120ms+，跟别的扇出交错在一起。

所以**不能按日志相邻关系判断"哪几条属于同一次点击/同一次扇出"**。要判断同一实例是否被并发重入，看的是"前后两次结果之间有没有足够时间落地"——见 9.3。

## 9.2 破阴是 7 个里唯一"沉降超时"最严重的那个

`ready: elapsed` 一行的 `stable` 判定（`ios2-web-boot.js:748`）：`stable: !forced && !sceneError`，
`forced = elapsed >= IOS2_STARTUP_MAX_SETTLE_MS = 1200`（`:15`）。

| 归属 | elapsed | stable | 判读 |
|---|---|---|---|
| 21电阴 / 22红血 | 403ms / 500ms | yes | 正常静下来 |
| 24潮阴 | 1179ms | yes | 卡着 1200 上限但赶上 |
| **25破阴** | **1666ms** | **no** | **超时强制发 ready** |
| 26混阴 | 439ms | yes | 正常 |
| 27黑阴 | 1330ms | no | 超时 |
| （23甜阴或24潮阴之一） | 1603ms | no | 超时 |

> 归属说明：`ready:` 行**不带账号身份**（`GameViewportInstance.swift:809`），上表是用
> `loading game document → WebGL webgl-context-recovery-installed → game document loaded → 本实例的 frame rate apply` 这条链倒推的。
> 25破阴的落点最硬：行 116/117（doc-loaded）+ 行 118（`25破阴 deferred:15`）紧接行 119 `ready: elapsed=1666ms stable=no`。

**含义**：破阴发 ready 的时候，资源**确实还在路上**（3/7 个实例都这样，多开 + 启动槽位串行是常态）。
"画面没加载完成"在那一刻是**真实状态**，不是渲染坏了。

## 9.3 决定性的新发现：主循环被同一实例连续拆建

数 `ok-restart`（= 走 `pause → 等 120~200ms → _setAnimFrame → resume`，**一次主循环拆建**）：

| 实例 | deferred | noop | **ok-restart** |
|---|---|---|---|
| 21电阴（焦点） | 1 | noop:30 ×7 | **0** |
| 22红血 | 1 | 5 | 2 |
| 23甜阴 | 3 | 3 | 2 |
| 24潮阴 | 3 | 4 | 1 |
| **25破阴** | 4 | 1 | **3** |
| 26混阴 | 5 | 1 | 2 |
| 27黑阴 | 7 | 0 | 1 |

两条硬结论：

**(E1) 每个非焦点实例都至少被拆一次，焦点实例 0 次。**
21电阴 全程 `noop:30`（7 次），其余 6 个全部出现 `ok-restart:15`。
对照 `GameViewportInstance.swift:771` —— bootstrap 给**所有**实例写的都是
`frameRate: TargetFrameRate.current().rawValue`（= 用户设的 30），**与焦点无关**。
于是引擎按 30 起，起来后的第一次仲裁必须把它降到 15 → **注定多拆一次主循环**。
这不是偶发，是 100% 复现的结构性浪费。

**(E2) 同一实例会被并发下发两次，两次都判为 `ok-restart`。**
证据（同一实例、几乎同时、两条 `ok-restart:15`）：
- 行 138 与 140 —— `23甜阴.bin`
- 行 171 与 175 —— `25破阴.bin`
- 行 174 与 176 —— `26混阴.bin`

`ok-restart` 的判据是 `g.config.frameRate !== fps`。如果第一次已经落地（写过 15），第二次必然 `noop`。
**两条都 `ok-restart` ⇒ 第二次读值的时候第一次还在 `await 120ms` 里没写。**
而 `applyFrameRate`（`GameViewportInstance.swift:560-586`）是 `Task { }` 火忘、**没有任何串行化**，
触发方 `reapplyEnergyPolicy()` 又会在**每个实例 ready 时**（`LobbySessionModel.swift:947`）和每次 `focus()` 时全量重放一遍
（7 个实例在 ~1s 内陆续 ready ⇒ 扇出间隔 < 120ms，正好落在重叠窗口里）。

→ **一个窗口在启动期会被反复"掐断主循环 120~200ms"**。破阴 3 次、23甜阴 2 次、26混阴 2 次。

## 9.4 把两次症状串起来

| 症状 | 触发 | 共同因子 |
|---|---|---|
| 上一次：点击窗口后元素不全 | 焦点切换 → 7 实例同时重下发 | `applyFrameRate` 的 pause→120ms→重启 |
| 这一次：破阴启动期先缺后全 | 实例 ready → 全量重下发（多次重叠） | 同上 |

**目前唯一被证实在"画面异常"时确实发生过的宿主动作，就是主循环拆建。** 两次症状都能由它 + 资源在途解释。

## 9.5 还差哪一条证据（这份粘贴被过滤过，不能当"没发生"）

这份 log 里**完全没有** `[cdn]` / `[scheme]` / `[js]` / `[pool]` / `[hsdk]` 这几类行，
而上一份完整日志里它们有几百条 —— 说明这次是**按关键字过滤或截取**过的。所以下面这些"没看到"**不能算证据**：

- `[instance] render integrity degraded (startup|watchdog): missing=N/M texture=x material=y`
- `[instance] WebGL pvr-recovery-scheduled / pvr-recovery-start / pvr-recovery-complete`
- `[instance] WebGL texture-upload-failed: …`

而这几条正是"**画面从缺变成全**"的直接执行者（`ios2-web-boot.js:1968-1971` → `scheduleASTCPVRRecovery` + `markSceneRenderDataDirty`）。
**下一次请给未过滤的完整日志**，或者直接在控制台里跑：

```
log stream --predicate 'process == "潮音之王"' --info | grep -E 'render integrity|pvr-recovery|texture-upload|webgl-|frame rate apply|ready:'
```

（Xcode 控制台里把过滤框清空也行 —— 关键是别只留 `[instance]`。）

## 9.6 两个已定位的宿主侧缺陷（等你发话再改）

**缺陷 A：bootstrap 写死的帧率与焦点无关** —— `GameViewportInstance.swift:771`
```swift
frameRate: TargetFrameRate.current().rawValue,   // 恒为「焦点档」，非焦点实例注定要多拆一次
```
改法：实例创建/reboot 时按"它此刻应不应该满帧"传值（焦点档 or `TargetFrameRate.idleFallback`）。
代价：0；收益：启动期每个非焦点实例少一次主循环拆建。**也可以直接当 A/B 实验**——如果改完症状消失，就坐实了 9.4。

**缺陷 B：`applyFrameRate` 无串行化** —— `GameViewportInstance.swift:560`
`Task { }` 火忘 + 内含 ≥120ms 的 await ⇒ 同一实例并发重入。
改法二选一：① 每个实例持一个 `inFlight` 标记，收到新目标时只记"最后一次目标"，等当前这次落地后再补发；
② 更彻底：把 `ok-restart` 路径改成**只在档位真的不同**时执行，并让 `reapplyEnergyPolicy` 对同一实例合并同一轮内的重复下发。
代价：中等（要小心不要把"改得动"这件事弄坏，`README.md:23` 那条回执语义得保住）。

⚠️ 注意：**先改代码就再也拿不到复现了**。建议顺序是「先按 9.5 抓一次完整日志 → 再改 A → 复跑观察」。

---

# 追加二：为什么"缺了也没有日志"——静默路径在引擎里

**问题**：元素缺失时 `render integrity degraded` / `texture-upload-failed` 一条都没有，"和 PVR 纹理有关系吗？"

**结论：有关系，但不是"PVR 上传失败"，而是"PVR 贴图后到"。后者在引擎里是一条完全静默的正常路径。**

## 10.1 引擎源码里那条静默路径（本仓自带引擎，可直接复核）

`ios-cocos/cocos-project/src/ios2-web-cocos2d.js`，`cc.Sprite.prototype._applySpriteFrame`：

```js
_applySpriteFrame: function (t) {
  if (this.isValid) {
    var e = t && t.isValid && t.getTexture();
    e && !e.loaded && t.off("load", this._applySpriteSize, this);
    var i = this._spriteFrame, n = i && i.isValid, r = n && i.getTexture();
    (e !== r && this._updateMaterial(),
      r && r.loaded
        ? this._applySpriteSize()                       // 纹理在 → 正常出图
        : (this.disableRender(),                        // 纹理不在 → 关掉自己的渲染
          n && i.once("load", this._applySpriteSize, this)));  // 挂一次性监听，等它到
  }
}
```

同文件的 `disableRender()`：

```js
disableRender: function () {
  this.node._renderFlag &= ~(c.FLAG_RENDER | c.FLAG_UPDATE_RENDER_DATA | c.FLAG_POST_RENDER);
}
```

**只改一个位标志，不打日志、不发事件、不抛错。** 于是：

| 时刻 | 发生什么 | 日志里 |
|---|---|---|
| 贴图还没到 | 该节点 `disableRender()` → 画面上**缺这一块** | **什么都没有** |
| 贴图到了（`texture.loaded = true` + `emit('load')`） | `_applySpriteSize()` → 这一块**自己补回来** | **什么都没有** |

而我们的 PVR 上传器正是这么触发的（`ios2-web-boot.js:1689-1690`）：
```js
textureAsset.loaded = true;
textureAsset.emit('load');
```

→ **"先缺后全"在这套引擎里就是一条正常路径，且天然零日志。** 用户看到的现象与它逐字吻合。

## 10.2 那 `texture-upload-failed` 和自检为什么都抓不到？

两条都只能抓"失败的中间态"，抓不到"还在路上的中间态"：

- **`texture-upload-failed`**（`:1702-1713`，`_nativeAsset` setter 的 catch 分支 `:2160-2166`）只在 **`uploadASTCPVRTexture` 抛异常**时触发。
  那条路上 `textureAsset.loaded` 停在 `false`，所以**必然**连带留下 `render integrity degraded`。**两条都没有 ⇒ 可以排除"PVR 上传失败"这一档。**
- **`render integrity degraded`**（`:1931-1973`）的采样节奏是 **`sendReady` 后 +4s 首采，此后每 12s 一次**（`:1978-1991`）。
  "后到"这种缺块通常只持续几百毫秒到几秒 → **极大概率整个落在两次采样之间**，采样时 `texture.loaded` 已经 `true` → 报 0。
  另外它只统计"节点上挂了带 `spriteFrame` 的组件且 `texture.loaded === false`"：
  - `cc.Label`（文字/数字）、`cc.Graphics`（矢量/FGUI 图元）、`sp.Skeleton`（骨骼特效）**都不产生 `spriteFrame` 组件 → 缺了永远不报**；
  - 只有 `cc.Sprite`（图标、立绘、活动图、FGUI 的 Image/Loader）才在覆盖范围内。

> 所以"缺了没日志"**不能**推出"与 PVR 无关"。恰恰相反：**最符合描述的就是 PVR 后到这条静默路径**；
> 而"上传失败"那一档已经被日志反证排除。

## 10.3 为什么偏偏是 PVR 更容易"后到"

> ⚠️ 本节第 3 条的原始写法（"PVR 体积通常大于同图 PNG"）已被 §11 的实测**推翻并修正**，见下方。

1. **这套 WebRuntime 把贴图格式钉成了 `.pvr`**：`cc.macro.SUPPORT_TEXTURE_FORMATS = ['.pvr']`（`ios2-web-boot.js:2263`），
   拿不到 PVRTC/ASTC 直接抛错、**PNG/WebP 兜底被显式关掉**（`:2337-2339`）。
   也就是说贴图全部走"CDN 下载 → 自定义 ASTC 解析 → 手工 `compressedTexImage2D` 上传"这条自建链路，比 PNG 多好几跳。
   （⚠️ 严格说：引擎**默认**的表里 `.pvr` 排在 `.png` 之前，本来也会选 `.pvr`；这一行的实际作用是**堵死 PNG 兜底**。详见 §11.2。）
2. **下载侧多开时会排队合并**：`CDNAssetStore.data(for:)` 对同一 URL 只发一次网络请求，其余实例 `await downloads[key]`
   （`CDNAssetStore.swift:186-191`）。7 个实例同时要同一批 PVR ⇒ 除第一个外都在等同一个 Task。
3. **~~PVR 体积通常大于同图 PNG~~ —— 实测相反**：全网取样 30 张游戏贴图，PNG 合计只有 PVR 的 **0.65 倍**
   （单张从 0.12× 到 1.67× 不等）。**下载窗口上 PVR 并不吃亏，甚至更吃亏的是 PNG**。
   当初钉死 PVR 的真正理由是**显存**，不是带宽 —— 见 §11.3。
4. 上传本身是**同步**的（`gl.compressedTexImage2D` 直接在 `_nativeAsset` setter 里跑，主线程），多开时 7 个进程一起上传会互相拖慢。
   这一条依然是 PVR 相对 PNG 的真实劣势面。（PNG 走 WebKit 原生解码 + `texImage2D`，不经 JS 主线程。）

对照上一份完整日志（`/Users/gg/log.txt`）：点击扇出（行 1383）**紧跟在** `yuling_1401` 的
`config/index/import/*.json + native/*.bin + native/*.pvr` 下载（行 1348-1380）之后 ——
也就是"点击 → 游戏开始加载新区资源（含 PVR）→ 贴图后到 → 局部缺块 → 到齐后自补"。
**这一条链不需要"主循环拆建"参与就能解释症状。**

> 结论修正：§6/§9.4 里排在首位的"主循环拆建"仍是被日志确证的**真实浪费**（每个非焦点实例启动时注定多拆一次、且会并发重入），
> 但**它不再是解释"元素不全"所必需的**。PVR 后到这条静默路径更简洁、且零日志，优先级应排到它前面。

## 10.4 一分钟定性（要趁"正缺着"的那一刻）

在**肉眼看到缺块的那一刻**，对该窗口执行（引擎未重建时入口一直在）：

```js
window.__ios2RenderIntegrityCheck()
```

（`ios2-web-boot.js:1992`，会强制上报 `render` 事件 → 原生侧打
`[instance] render integrity degraded (manual): missing=N/M texture=x material=y`，`samples` 里直接列出贴图名）

| 返回 | 含义 |
|---|---|
| `missingTexture > 0` | **确认是贴图后到**；看 `samples` 里的文件名，若是 `*.pvr` 就是实锤 |
| `missingMaterial > 0` | 材质丢了（上下文/device 重建过的典型症状） |
| `missing = 0` 而画面确实缺 | 缺的不是 Sprite 贴图 → Label 文字 / Spine / Graphics / FGUI 非 Sprite 部件，落在自检盲区 |

⚠️ **必须趁缺着的时候按**。等它自己补全了再按，必然是 0 —— 这也正是 12s 定时采样抓不到的原因。

## 10.5 建议的修法（把"静默"变成"可观测"）

**首选：事件驱动记账，替掉 12s 定时遍历。**
在 `ios2-web-boot.js` 里给 `cc.Sprite.prototype._applySpriteFrame` 包一层：当走到
`!r.loaded` 那支（也就是 `disableRender()` 被调用）时，把 `{node 路径, 贴图 URL}` 记进一个集合；
`emit('load')` 补回来时从集合里删掉。于是**任何时刻**都能回答"现在有几个节点因为贴图没到画不出来、分别是哪几张图"，
而且是**零遍历成本、100% 覆盖**，不依赖采样运气。上报仍在 `missing > 0` 时触发即可（不会刷屏）。

**次选：把采样点挪到关键时刻** —— `pvr-recovery-complete` 之后、场景切换之后（已有 `:962` 的 `scene transition`）、
`ready` 之后连续几次（1s/2s/4s）各采一次，而不是固定 12s。

**另外**：把 `cc.Label` / `sp.Skeleton` / `cc.Graphics` 纳入自检分母，否则文字和特效类缺失永远不可观测。

---

# 11. PNG 到底能不能用？—— CDN 实测与"为什么钉死 PVR"的真相

> 起因：用户提示「cocos2d 也支持 png 纹理，别人 webkit 几乎都是用 png 纹理」。
> 这条提示方向正确且重要，本节把它查到底。**结论有两半：PNG 确实可用（CDN 上有），
> 但当初钉死 PVR 不是拍脑袋 —— 是 12× 显存差。两者都要说清楚。**

## 11.1 一句话结论

| 命题 | 判定 | 证据 |
|---|---|---|
| 别人用 PNG，我们钉 PVR，我们是异类 | ✅ 成立 | §11.2 |
| CDN 上**没有** PNG，所以只能走 PVR | ❌ **不成立** | §11.3，30/30 取样命中 |
| 钉 PVR 只是为了省流量 | ❌ **反了**，PNG 平均小 35% | §11.3 |
| 钉 PVR 是为了省显存 | ✅ 成立，差 **11.9×** | §11.4 |

## 11.2 三处"钉死 PVR"的代码，实际作用各是什么

### (1) `cc.macro.SUPPORT_TEXTURE_FORMATS = ['.pvr']`（`ios2-web-boot.js:2263`）

引擎默认值是（`ios2-web-cocos2d.js:43268-43276`）：

```js
cc.macro.SUPPORT_TEXTURE_FORMATS = [".pkm",".pvr",".webp",".jpg",".jpeg",".bmp",".png"];
```

选格式的逻辑在 `cc.Texture2D._parseExt`（`:24385-24423`）——**取支持列表里下标最小的**：

```js
for (var h = 0; h < n.length; h++) {
  var u = n[h].split("@"), _ = u[0];
  _ = p.extnames[_.charCodeAt(0) - 48] || _;      // '0'→extnames[0], '5'→extnames[5]
  var f = c.indexOf(_);                            // c = SUPPORT_TEXTURE_FORMATS
  if (-1 !== f && f < a) { ...a = f; s = _;... }   // 越小越优先
  else r || (r = _);                               // 都不支持 → defaultExt
}
return { bestExt: s, bestFormat: o, defaultExt: r };
```

本项目每个 `cc.Texture2D` 的 `_native` 串都一样（实测 2037 个）：

```
"0_5@5,9729,9729,33071,33071,0,0,1"
```

即**两个候选**：`'0'`→`.png`、`'5'`→`.pvr`。

- **用默认表**：`.png` 下标 6、`.pvr` 下标 1 → `1 < 6` → `bestExt = '.pvr'`。
- **用覆盖后的 `['.pvr']`**：`.png` 不匹配（落进 `defaultExt`）、`.pvr` 下标 0 → `bestExt = '.pvr'`。

**⇒ 两种写法选出来的都是 `.pvr`。`SUPPORT_TEXTURE_FORMATS` 这一行并不改变"选哪个"，它的真实作用是
把 PNG 从"可选项"降级为"仅在无压缩扩展时兜底的 defaultExt"，配合 (3) 的抛错彻底堵死。**

### (2) `textureExtnames[5] = '.pvr'`（`ios2-web-boot.js:2100`）——**已经是死代码**

官方 2.4.9 引擎的 `extnames` 表（`ios2-web-cocos2d.js:24375-24383`）本来就是：

```js
extnames: [".png", ".jpg", ".jpeg", ".bmp", ".webp", ".pvr", ".pkm"]
```

**下标 5 已经是 `.pvr`。**这行补丁写在 `c7a8b26 webkit cocos2d js 更新官方的2.4.9引擎` 之前（老引擎确实缺 index 5），
换成官方引擎后**语义相同、纯冗余**。注释"supplied Web engine omits index 5"已过期。

### (3) 硬抛错（`ios2-web-boot.js:2337-2339`）——**这才是真正生效的闸门**

```js
if (!pvrtc && !astc) {
    throw new Error('PVRTC or ASTC is required; PNG/WebP fallback is disabled');
}
```

而 `pvrtc` 在无 PVRTC 扩展的设备上还会被 alias 成 ASTC（`:2343-2346`），
所以实际生效条件是"**连 ASTC 都没有**"。

> **小结**：真正阻止 PNG 的只有 (3)；(1) 是配套声明；(2) 是无害冗余。
> README（`ios-cocos/README.md:93-95`）也明写 "Texture candidates remain restricted to `.pvr` … No PNG or WebP fallback is used" —— 是**有意的设计决策**，不是疏漏。

## 11.3 CDN 上到底有没有 PNG？—— **有，而且是同一份资源**

### 证据 A：同一个 uuid、**同一个版本号**，两种扩展名都在

本地 CDN 缓存（`~/Library/Application Support/GameLobby/CDN/index.json`，8683 条）里直接看到：

```
    2256  https://xxz-xyzw-res.hortorgames.com/remote/icons/native/64/642bba07-b7d5-4c54-a0a3-1235f54e8913.449a6.pvr
    4014  https://xxz-xyzw-res.hortorgames.com/remote/icons/native/64/642bba07-b7d5-4c54-a0a3-1235f54e8913.449a6.png
```

**uuid 相同、版本哈希 `449a6` 相同，只是扩展名不同** ⇒ 原生文件名规则就是
`native/<uuid 前2位>/<uuid>.<ver>.<ext>`，`<ext>` 由选格式逻辑决定。**把 `.pvr` 换成 `.png` 就是另一份真实存在的文件。**

### 证据 B：游戏贴图也一样（不是只有 icons/launcher）

从缓存里随机抽 30 张**游戏 bundle**的 `.pvr`，把扩展名改成 `.png` 直接 GET：

```
探测 30 个游戏贴图（GET 实际字节）：png 可用 30/30
合计  pvr 3,319,712 B   png 2,149,557 B   → png/pvr = 0.65
```

单张样本（含最极端的两头）：

| 贴图 | pvr | png | png/pvr |
|---|---:|---:|---:|
| `ui_common` | 700,432 | 1,169,435 | 1.67 |
| `buff_dizzy` | 2,832 | 2,079 | 0.73 |
| `zhouyu7/…dcf2f08c` | 88,080 | 51,629 | 0.59 |
| `ui_battleResult` | 88,080 | 15,024 | 0.17 |
| `ui_holyBeastSkinSys` | 44,048 | 5,500 | 0.12 |

并校验返回体是真 PNG（魔数 `89 50 4e 47 0d 0a 1a 0a`），**未加密、可直接用**。

> **⇒ `throw new Error('… PNG/WebP fallback is disabled')` 里"disabled"是"被我们关掉"，不是"没有"。**
> 这条兜底路径在 CDN 侧是**通的**。

### 顺带纠正我上一版的一个判断

§10.3 原第 3 条写"PVR 体积通常大于同图 PNG"——**实测相反**：PNG 平均只有 PVR 的 **65%**，
很多活动图只有 12%~18%。就**下载窗口**而言 PVR 并不占优，这条不能用来解释"后到"。

## 11.4 那当初为什么非 PVR 不可？—— **显存，差 11.9 倍**

把本地缓存里 **全部 2046 张 `.pvr`** 的头部解出来（PVR 容器内是 ASTC）：

```
块尺寸分布: [('8x6', 2046)]        ← 全部统一，无例外
像素尺寸 Top: 512x512(367) / 80x80(184) / 512x256(183) / 1024x512(138)
             / 256x256(133) / 1024x1024(90) / 2048x2048(59)

缓存内 2046 张 PVR 贴图：
  ASTC 压缩态显存  220.5 MB   RGBA8888 解码后 2633.4 MB   放大 11.9×
  ×7 实例：        1543 MB  vs  18434 MB
```

- ASTC **8x6** 块 = 48 px/块 × 16 B/块 = **0.333 B/px**（GPU 上**保持压缩**，不展开）。
- PNG 必须解码成 RGBA8888 = **4 B/px**。

**同样的贴图集，7 个实例同时常驻：ASTC ≈ 1.5 GB，PNG ≈ 18 GB。** 这在 Mac 上是不可接受的。

> ⇒ **PVR-only 不是过度设计，是多开场景下的硬约束。** 用户的"别人都用 PNG"成立，
> 但那些产品通常是**单实例**；一旦 7 开，PNG 的显存账算不过来。
> （代价是 ASTC 8x6 有损，观感略低于无损 PNG —— 这是另一个话题。）

## 11.5 建议：把"换 PNG"当**诊断实验**做，不要当长期方案

因为 §11.3 证明 PNG 路径是通的，所以有了一个**极低成本、完全可逆的 A/B 判据**
——它能把"是不是 PVR 链路导致的缺块"一刀切开：

```js
// ios2-web-boot.js
cc.macro.SUPPORT_TEXTURE_FORMATS = ['.png'];   // 原 ['.pvr']  ← 临时
// 并把 :2337-2339 的 throw 注释掉
```

（不用动游戏脚本，只动我们自己的 WebRuntime；改完**必须 bump `buildTag`**）

| 观察结果 | 推论 |
|---|---|
| **缺块消失** | 实锤：问题在 PVR 自建链路（自定义下载器/解析/手工 `compressedTexImage2D`/恢复队列） |
| **缺块照旧** | 与贴图格式无关 —— 回到"异步加载时机 + 引擎静默 `disableRender()`"这个通用问题，重心转向 §10.5 的可观测性改造 |

**⚠️ 三条注意事项：**

1. **只在 2~3 开下跑**。7 开 + PNG 会直接顶到 18 GB 量级的显存，实验本身会引入新变量。
2. 这是**诊断**，不是结论。即使缺块消失，长期方案也要在"PNG 的显存账"和"PVR 的故障面"之间权衡，
   更可能是"**保留 PVR + 把静默路径变成可观测/可恢复**"，而不是切 PNG。
3. PNG 路径下 `installASTCTextureSupport` 注册的下载器/解析器与 `_nativeAsset` 拦截**全部不会被触发**，
   等于顺手验证了"这套自建链路是不是只在这条路上出事"。

## 11.6 本节对前面章节的修正

- §10.3 第 1 条：补注"引擎默认也会选 `.pvr`，该行的实际作用是堵死 PNG 兜底"。
- §10.3 第 3 条：**推翻**（PNG 反而更小）。
- §10.3 第 4 条：保留并强化 —— PVR 的手工同步上传**确实**是相对 PNG 的额外劣势面。
- §10.5 的"事件驱动记账"建议**不受影响**，反而是两条路径的共同必需项：
  无论 PVR 还是 PNG，`cc.Sprite._applySpriteFrame` 的静默 `disableRender()` 都在。

---

# 12. 定位分析路线（可执行）

> 用户提问「那现在要怎么定位分析呢」。本节是**行动方案**，按"先便宜、后昂贵"排序。
> 核心原则：**当前最大的障碍不是缺假设，是缺观测** —— 缺口本身被引擎设计成静默的（§10），
> 继续读日志只会得到"什么都没发生"。

## 12.0 先明确"已经排除了什么"（别重复查）

| 已排除 | 依据 |
|---|---|
| WebGL 上下文丢失 | 日志里无 `webgl-lost` / `webgl-fatal`，且 `render integrity degraded while WebGL context is lost` 未出现 |
| PVR 上传抛异常 | `texture-upload-failed` 需 `uploadASTCPVRTexture` 抛错才打（`:1702-1713`），一条都没有 |
| CDN 资源缺失 / 404 | 30/30 贴图 URL 直探 200（§11.3） |
| 贴图格式只有 PVR 可选 | **推翻**：CDN 同时有 `.png`（§11.3） |

**剩下三个候选，且都是"静默"的：**
1. **贴图后到**（PVR 链路更长 → 未 loaded 期间 `_applySpriteFrame → disableRender()`）
2. **补不上**（贴图到了但 assembler 不再进来，脏标记没打 —— 见 `ios2-web-boot.js:1910-1912` 的注释）
3. **非 Sprite 部件**（Label/Spine/Graphics 本来就落在自检盲区，缺了永远看不见）

## 12.1 第一优先：零改动，5 分钟内定性（推荐先做这个）

项目里**已经有**现成的调试入口：设置页 → **「Web 检查器」**（`SidebarSettingsView.swift:76`，
底层 `GameViewportInstance.swift:689` 的 `view.isInspectable`）。

**步骤：**

1. 打开「设置 → Web 检查器」，重启大厅（`isInspectable` 在 WebView 创建时生效）。
2. 正常多开、复现"点击后某个窗口缺块"。
3. **在那个缺块的窗口上右键 → 检查元素**，打开 Safari Web Inspector 的 Console。
4. 粘贴下面这段**只读探针**（不触发自愈，可反复按）：

```js
(() => {
  const gaps = [], stack = [cc.director.getScene()];
  while (stack.length) {
    const n = stack.pop(); if (!n) continue;
    (n._children || []).forEach(c => stack.push(c));
    if (n.activeInHierarchy === false) continue;
    const cs = n._components || [];
    for (const c of cs) {
      const name = (c.constructor && c.constructor.name) || '?';
      // ① Sprite 类：spriteFrame 的纹理没 loaded
      const f = c.spriteFrame;
      if (f && typeof f.getTexture === 'function') {
        const t = f.getTexture();
        if (!t || t.loaded === false) {
          gaps.push({ node: n.name, comp: name, kind: 'texture-not-loaded',
                      url: String((t && (t._nativeUrl || t.nativeUrl)) || '?').split('/').pop() });
          continue;
        }
        if (c._materials && c._materials.length === 0) {
          gaps.push({ node: n.name, comp: name, kind: 'no-material' });
        }
        continue;
      }
      // ② Label / Spine / Graphics：各自的"素材没准备好"
      if (name === 'Label' && c.font && c.font.spriteFrame &&
          c.font.spriteFrame.getTexture && c.font.spriteFrame.getTexture().loaded === false) {
        gaps.push({ node: n.name, comp: name, kind: 'font-atlas-not-loaded' });
      }
      if (name === 'Skeleton' && c._skeletonData === undefined) {
        gaps.push({ node: n.name, comp: name, kind: 'skeleton-data-missing' });
      }
    }
  }
  return { count: gaps.length, gaps: gaps.slice(0, 30) };
})()
```

**判据（这一步就能把候选 1/2/3 切开）：**

| 输出 | 结论 | 下一步 |
|---|---|---|
| `count === 0` 但肉眼确实在缺 | **候选 3**：缺的是 Label/Spine/Graphics 或 FGUI 非 Sprite 部件 | 走 §12.4 补探针 |
| 有 gap，`kind: 'texture-not-loaded'`，`url` 以 `.pvr` 结尾 | 落进**候选 1 或 2** | 走 §12.2 才能再分清 |
| 有 gap，`kind: 'no-material'` | 材质丢了 → 上下文/device 重建残留 | 查 `webgl` 链路 |
| 有 gap，`kind: 'font-atlas-not-loaded'` | 文字图集后到（此前完全在盲区） | 新发现，记下来 |

> ⚠️ **不要用 `window.__ios2RenderIntegrityCheck()` 做这个定性**：它有副作用
> ——`missing>0` 时会 `scheduleASTCPVRRecovery()` + `markSceneRenderDataDirty()`（`:1968-1971`），
> **当场把缺块补上**。它适合"看看能不能自愈"，不适合"看清现状"。

> ⚠️ 右键这个动作本身可能改变焦点 → 触发帧率仲裁 → 缺口自愈。
> 所以：**先右键把 Inspector 打开，再复现缺口**，然后直接在 Console 里按。

## 12.2 第二优先：把"静默"变成账本（一次性观测改造）

只有这一条能回答"缺了多久、缺的是哪几张、为什么没补上"。
**页面侧**（`ios2-web-boot.js`，不碰游戏脚本）：

1. **事件驱动记账，替掉 12s 定时遍历**（§10.5 已提）：
   包一层 `cc.Sprite.prototype._applySpriteFrame`，走到 `!loaded` 分支时把
   `{nodeId, url, t0}` 记进 `gapSet`；`emit('load')` 回来时删除并累计 `dwellMs`。
   任何时刻都能回答"现在缺几张、缺了多久、缺的是哪几张"。
2. **加脏标记计数**：hook `markSceneRenderDataDirty()`，记调用次数与时刻。
   **这是区分候选 1 / 2 的关键**：
   - 缺口存在、但**从未**触发过 `markSceneRenderDataDirty` ⇒ **候选 2（补不上）**
   - 触发过、且之后缺口消失 ⇒ **候选 1（后到，链路正常只是慢）**
3. **节流上报**：`gapSet` 由空变非空、由非空变空、以及最长每 2s 各一次
   `type:'render'`，字段加 `gapCount / worstDwellMs / urls / firstGapAt`。

**原生侧**（`GameViewportInstance`）：

4. **把 `render` 事件落盘**到现成的取证通道 `DiagnosticsLog.append`
   （`Sources/LobbyStorage/DiagnosticsLog.swift:29`，专为此设计）：
   ```
   [render-gap] 25破阴 count=3 worst=1420ms reason=watchdog urls=a.pvr,b.pvr
   ```
   ⇒ **用户不用再从 Xcode 控制台挑行粘贴**（此前两次白跑就是因为粘贴被过滤/截断）。
5. **补账号身份**：`ready: elapsed` 行（`GameViewportInstance.swift:809`）与
   `render integrity degraded`（`:1055`）目前都**不带 `account.fileName`**。

## 12.3 第三优先：三变量正交 A/B（定根因）

拿到 12.2 的账本后，用三次单变量实验把根因钉死。**每次只改一个变量、都要 bump `buildTag`。**

| 实验 | 改什么 | 缺块消失 ⇒ | 缺块照旧 ⇒ |
|---|---|---|---|
| **E-C** 单实例跑同样操作 | 不用改代码，只开 1 个窗 | 多开资源竞争（`CDNAssetStore` 同 URL 合并 `await`、GPU/磁盘竞争） | 与多开无关，重心在单实例加载时机 |
| **E-B** 强制 PNG | `SUPPORT_TEXTURE_FORMATS=['.png']` + 放开 `:2337-2339` 的 throw（§11.5） | PVR 自建链路（自定义下载/解析/手工 `compressedTexImage2D`/恢复队列） | 与贴图格式无关 |
| **E-A** 冻结帧率仲裁 | 让 `applyFrameRate()` 不拆主循环（所有实例同档） | 主循环 `pause→_setAnimFrame→resume` 期间丢渲染标记是元凶 | 与帧率仲裁无关 |

**建议顺序 E-C → E-B → E-A**：E-C 零改动、E-B 两行、E-A 最侵入。
⚠️ **每个实验都要等账本里"缺口再次出现"才算有效复现** —— 否则实验没有判据。

## 12.4 顺带要补的观测盲区

当前自检 `collectRenderIntegrity()`（`ios2-web-boot.js:1876-1908`）只数
"挂了 `spriteFrame` 的组件"，所以：

- `cc.Label`（BMFont 图集后到）→ **永远不报**
- `sp.Skeleton`（骨骼数据/贴图）→ **永远不报**
- `cc.Graphics`（矢量/FGUI 图元）→ **永远不报**

如果 12.1 的探针输出 `count === 0`，就说明缺的正在这些盲区里 ——
那必须把这三类纳入分母，否则"缺块"这件事永远无法被观测。

## 12.5 一句话执行摘要

> **今天就能做的**：开「Web 检查器」→ 复现 → 缺的那个窗口 Console 粘只读探针
> → 看 `kind` 是 `texture-not-loaded` 还是 `count===0`。
> **这一步就能把三个候选砍到两个。**
> 之后再决定是投观测改造（§12.2）还是直接上 A/B（§12.3）。

---

# 13. 第三次实测（`/Users/gg/log.txt`，buildTag 2026-09-19.32，webInspector=on）

> 现象：测试 `27黑阴.bin` 时出现画面缺失。
> 这一份**终于是完整日志**（3648 行）：`[cdn] 42` / `[scheme] 685` / `[sync] 561` / `[instance] 631` / `[hsdk] 1092` 都在。
> 前两次"什么都看不到"的原因是**粘贴被过滤**，不是日志没打。

## 13.0 一句话结论

**找到了"后到"的实体，也再次确认了"静默"：**

> 一次点击被同步回放给 6 个其它窗口 ⇒ **7 个窗口同时打开同一个新 UI、同时各自加载同一批新资源**；
> 其中两张 **2048×2048 的 ASTC 立绘（各 1,400,848 B）** 在 `3015/3018` 发起、`3020/3021` 才到齐
> ⇒ **面板先出来、立绘后补** —— 这就是"画面元素不全 → 过一会全了"。
> 而整个过程**仍然零条 `render integrity` 日志**（12s 定点采样抓不到几百 ms 的瞬态）。

## 13.1 还是一片静默（与 §10 的预测一致）

| 关键日志 | 条数 |
|---|---|
| `render integrity degraded` | **0** |
| `texture-upload-failed` | **0** |
| `webgl-lost` / `webgl-fatal` / `context lost` | **0** |
| `reload` | **0** |
| `[sync] … miss>0` | **0**（回执里 `miss=0` 贯穿全程） |

⇒ 排除项又多了一层证据：**不是上下文丢失、不是上传失败、不是 CDN 失败、同步没丢事件**。
⇒ 同时**再次证明 §12 的判断**：缺口是静默的，不改造观测就永远读不出来。

## 13.2 主因证据：一次点击 = 7 个窗口各开同一 UI、各加载同一批资源

### (a) bundle 请求的爆发与"7 连"

`[scheme] bundle URL rewritten` 全量 678 条，按 bundle 统计被请求次数：

```
被请求 7 次 →  75 个 bundle      ← 全部 7 个实例都开了
被请求 5 次 →   4 个
被请求 4 次 →   4 个
被请求 3 次 →  13 个
被请求 2 次 →  20 个
被请求 1 次 →  38 个
```

其中 **`ui_holyBeastSkinPreview` / `ui_playerinfo_common` / `ui_holyBeastSkinSys` 都是 7 次**
（第 2955-2987 行集中爆发）→ **七个窗口同时进了"皮肤预览 / 玩家信息"面板**。

> ⚠️ 注意：`[scheme]` 走的是 `LobbyLog.verbose`（`GameResourceSchemeHandler.swift:353`），
> 而 verbose/debug 级在 macOS 统一日志下**可能被丢弃**，所以"次数"只能当**下界**看，
> 不能断言"只有 1 个实例要过它"。但**没有实例身份**这一点是确定的（见 §13.4）。

### (b) "后到"的实体：两张 2048² ASTC 立绘

`[cdn]` 显示，这一轮**只有两个资源真的走网络**（其余都命中本地 CDN 缓存）：

```
3015  game network download started:    hero_hb106/native/de/de144869-…b1cc6.pvr
3018  game network download started:    hero_hb112/native/7b/7bada7e8-…3611b.pvr
3020  game download completed and cached: hero_hb106/…b1cc6.pvr   (1400848 bytes)
3021  game download completed and cached: hero_hb112/…3611b.pvr   (1400848 bytes)
```

**尺寸对得上就是实锤**：

```
1400848 B − 16 B（ASTC-in-PVR 头） = 1400832 B
ceil(2048/8) × ceil(2048/6) × 16 B = 256 × 342 × 16 = 1400832 B   ✅ 精确吻合
```

⇒ 这是两张 **2048×2048、ASTC 8x6** 的立绘（每张显存 1.34 MiB，代价不大）。
**代价不在显存，在"到达时刻"** —— 面板已经在画了，这两张图还在路上。

### (c) 关键：下载合并了，**解析+上传没有合并**

`CDNAssetStore` 对同一 URL 只发一次网络请求（`[cdn] game network download started` 每个 URL 只出现一次），
其余实例 `await downloads[key]`。**但等下载完成后，7 个进程各自**：

1. 跑一遍自定义 ASTC 解析（`parseASTCPVRBuffer`，1.4 MB × 2）
2. 在主线程同步 `gl.compressedTexImage2D` 上传（1.4 MB × 2）

⇒ **同一份字节被解析 + 上传 7 遍。** 谁最后完成，谁的画面就最后补全。

### (d) 时间线（行号，本日志无时间戳）

```
2736-2750  [scheme] 新 bundle 开始出现（xiaobing2_qingtongniao / ui_heroList）
2743/2748  27黑阴 收到回放点击的 ack
2778       [sync] 回执日志达上限(400)，之后 ack 不再逐条打
2801-3000  ★ bundle 请求爆发区间（93 次）
2955-2987  ui_holyBeastSkinPreview / ui_playerinfo_common / hero_hb1xx 集中请求
2980-2987  hero_hb106 / hero_hb112 的 index.js（各 1 次）
3015/3018  ★ 两张 1.4 MB .pvr 发起网络下载
3020/3021  ★ 两张 1.4 MB .pvr 到达      ← 这之前，面板的立绘是空的
3024-3035  点击继续（破阴 mousedown/mouseup），扫描 7 实例
…
3587-3599  ★ 27黑阴 成为操作源（用户开始点黑阴的窗口）
3595       27黑阴.bin 焦点=on → ok-restart:30   ← 黑阴自己的主循环被拆了重建
3604       27黑阴.bin 焦点=on → noop:30
3614       27黑阴.bin 焦点=off → ok-restart:15  ← 离开焦点，又拆一次
3617       25破阴.bin 焦点=on → ok-restart:30
```

## 13.3 次因：点进哪个窗口，**那个窗口自己**的主循环就被拆建

这次带身份坐实的"点击指纹"：

```
3587  [sync] publish 27黑阴 mousedown → 6 目标      ← 用户点进黑阴
3595  [instance] frame rate apply: 27黑阴.bin 焦点=on  → ok-restart:30   ← 黑阴被拆重
3614  [instance] frame rate apply: 27黑阴.bin 焦点=off → ok-restart:15   ← 黑阴又被拆
```

`ok-restart` 走的是 `frameRateApplyScript`（`GameViewportInstance.swift:531-555`）：
`g.pause()` → **等 ≥120 ms** → 改 `config.frameRate` → `g._setAnimFrame()` → `g.resume()`。
**被点的那个窗口，是自己被拆。**

旁证：`3596/3615/3618-3621` 紧挨着这些 restarts 出现
`RBS assertion 'WebKit Media Playback'` 失败（PID 6135/6133/6126）——
说明 `pause()` 这条路上确实在触发 WebKit 的媒体/隐藏处理。

### 顺手修正上一轮的一个过度判断

**帧率扇出不是"泄漏"**：511 条 `frame rate apply` 里

```
404  off→noop:15     69  on→noop:30     22  off→deferred:15
 13  off→ok-restart:15     3  on→ok-restart:30
```

**真重建只有 16 次**，其余全是 `noop` / `deferred`。
且 `noop` 是在**暂停之前**就 early-return 的（`:539`：`if (g.config.frameRate === fps) return 'noop:'+fps`），
所以 495 条 noop/deferred 只花一次 `callAsyncJavaScript` 往返，代价很小。
**触发频率 ≈ 每次点击一次全量扫描**（≈62 次点击 ↔ ~73 次扫描），属预期行为，不是 bug。

⇒ **E1（"每个非焦点实例启动时必被多拆一次"）仍然成立且仍是真实浪费，但它不是本次症状的原因。**

## 13.4 卡在哪：三处身份缺失，导致无法闭环

| 缺什么 | 位置 | 后果 |
|---|---|---|
| `[scheme] bundle URL rewritten` 无账号 | `GameResourceSchemeHandler.swift:353`（handler **是每实例一个**，`GameViewportInstance.swift:125`，所以**补得上**） | 无法回答"是哪个窗口请求了 `hero_hb106`" |
| `[cdn] game network download …` 无账号 | `CDNAssetStore` | 无法把"1.4 MB 后到"归到某个窗口 |
| 实例 ↔ WebContent PID 无映射 | 全日志（PID 6126/6128/6129/6132/6133/6134/6135/6136） | RBS 断言、WebContent 异常都定位不到窗口 |
| 缺口本身无日志 | `collectRenderIntegrity` 12s 定点采样 | 几百 ms 的瞬态抓不到（§13.1 已 0 条） |

## 13.5 结论与下一步

**本次"黑阴画面缺失"最符合证据的解释：**

> 你在破阴点的操作被同步回放给另外 6 个窗口 ⇒ **7 个窗口同时进入"皮肤预览 / 玩家信息"面板**并各自去加载新资源；
> 其中两张 **2048² ASTC 立绘（1.4 MB × 2）** 只有网络一份、却要在 7 个进程里各解析各上传一遍。
> **黑阴 是最晚完成的那个**（它是最后一个启动的实例，`[pool] startup slot released: 27黑阴.bin` 在 1145 行，
> 晚于其它 6 个），所以它缺得最明显、补得最晚。

**次因**：`3587-3614` 你连续点进/点出黑阴窗口，**黑阴自己的主循环被拆建了 2 次**
（`ok-restart:30` / `ok-restart:15`），每次 `pause` + ≥120 ms 空窗。这两条链**可能叠加**。

**下一步（只差一层就闭环，全在 §12.2）**：

1. 给 `[scheme] bundle URL rewritten` 加 `account.fileName`（handler 本来就是每实例一个，零成本）。
2. 给 `[cdn] game network download started/completed` 带上**请求方账号**（`CDNAssetStore` 的调用方已经在栈上）。
3. 给 `[instance]` 日志加 WebContent **PID**，把 PID→账号映射打出来（RBS/WebContent 异常就能归位）。
4. 上**事件驱动缺口账本**（hook `_applySpriteFrame` 的 `!loaded` 分支 + `markSceneRenderDataDirty` 计数），
   落盘到 `DiagnosticsLog`（前缀 `[render-gap]`）。

**顺带可以省掉的改动**：`E-A`（冻结帧率仲裁）优先级可以下调 —— 本次证据显示扇出是"每点击一次"，代价小。
`E-B`（强制 PNG）**优先级上调** —— 因为现在已知瓶颈是"7 个进程各自解析+上传同一份 PVR"，
而 PNG 路径由 WebKit 自己解码、不经我们的解析器，**正好能验证这一条**。

---

# 14. 观测改造落地（buildTag `2026-09-20.33`）

> 按 §13.4 的四项缺口逐条补上。**只加观测，不改任何判定逻辑**——不碰自动重载阈值、
> 不碰帧率仲裁、不碰贴图格式。改动全部在宿主侧与 `ios2-web-boot.js`（我们自己的 WebRuntime），
> **游戏脚本一行没动**。

## 14.1 补了什么

| # | 缺口 | 改法 | 位置 |
|---|---|---|---|
| 1 | `[scheme]` 请求无账号 | handler 本来就是**每实例一个**，在构造时注入 `owner` | `GameResourceSchemeHandler.swift` / `GameViewportInstance.swift:125` |
| 2 | `[cdn]` 下载无请求方 | `ResourceProviding.data` 加 `requester` 参数透传 | `EngineContracts.swift`、`CDNAssetStore.swift`、`GameResourceSchemeHandler.swift` |
| 3 | 实例 ↔ WebContent PID 无映射 | 首次 `ready` 时打一行 `webcontent pid=` | `GameViewportInstance.logWebProcessIdentity()` |
| 4 | 缺口本身无日志 | **事件驱动缺口账本** + 落盘 | `ios2-web-boot.js` + `PageEvent.renderGap` + `handle(pageEvent:)` |

## 14.2 新的日志形态（对照）

```diff
- [scheme] bundle URL rewritten: /remote/hero_hb106/index.e1980.js -> ...
+ [scheme] 27黑阴.bin bundle URL rewritten: /remote/hero_hb106/index.e1980.js -> ...

- [cdn] game network download started: https://…/hero_hb106/native/de/…b1cc6.pvr
+ [cdn] game network download started: https://…/hero_hb106/native/de/…b1cc6.pvr (by=27黑阴.bin)
- [cdn] waiting for shared download: https://…/hero_hb106/…
+ [cdn] waiting for shared download: https://…/hero_hb106/… (by=21电阴.bin)
- [cdn] game download completed and cached: https://… (1400848 bytes)
+ [cdn] game download completed and cached: https://… (1400848 bytes) (by=27黑阴.bin)

- [instance] ready: elapsed=403ms stable=yes
+ [instance] ready: 27黑阴.bin elapsed=403ms stable=yes
+ [instance] 27黑阴.bin webcontent pid=6135        ← 新增：PID 对得上窗口了

- [instance] render integrity degraded (watchdog): missing=…
+ [instance] render integrity degraded (watchdog): 27黑阴.bin missing=…
```

**`[cdn] waiting for shared download … (by=X)` 这次是重点**：同一个 URL 会出现**多条**、每条的
`by=` 不同，这就是"N 个实例在等同一份下载"的直接读数；谁的 `by=` 出现得最晚，
谁的这块资源就最后到手。

## 14.3 事件驱动缺口账本（本次的核心）

### 为什么必须事件驱动

`collectRenderIntegrity()` 是**定时**采样（+4s 首采、此后每 12s），而缺块是**几百毫秒到几秒的瞬态**——
三次实测里 `render integrity degraded` 一条都没出现，不是没缺，是采样永远错过。

引擎在贴图未就绪时走的是（`ios2-web-cocos2d.js` 里 Sprite 的定义，offset ≈ 1196794）：

```js
r && r.loaded ? this._applySpriteSize()
              : (this.disableRender(), n && i.once("load", this._applySpriteSize, this))
```

而 `disableRender()` 只清 `_renderFlag`，**不 log / 不 emit / 不 throw** —— 零痕迹。

### 实现

在 `_applySpriteFrame`（赋 spriteFrame 时）与 `_validateRender`（渲染前校验时）各包一层：

| 时机 | 动作 |
|---|---|
| 走到未就绪那一支 | 记进 `gapLedger`（节点 uuid → `{node, texture, since, url}`），给该贴图挂**一次性** `load` 监听 |
| 贴图 `load` 回来 | 销账，累计存活时长（`worstMs`） |
| 节点已销毁 | 记账前/上报前剪掉（否则资源 404 会让 `openCount` 永远 >0 无限刷） |

**上报节流**：只在「由空变非空」「由非空变空」两个跳变点立即发；持续缺着时最多每 5s 一条。
**零遍历成本**：健康路径（贴图已 loaded）只做几次属性读取就 `return`。

### 新增事件字段（`type: "render-gap"`）

| 字段 | 含义 |
|---|---|
| `open` | 当前仍缺着的节点数（销账后应回到 0） |
| `total` | 本次会话累计发生过的缺口次数 |
| `worstMs` | 单个缺口最长存活时长——"缺了 300ms"和"缺了一分钟"是完全不同的病 |
| `reason` | `gap-open` / `gap-close` / `throttle` |
| `urls` | 缺口贴图文件名（前 6 个），**直接指认是哪几张图** |
| `dirtyMarks` | **`markSceneRenderDataDirty()` 调用次数** |

### `dirtyMarks` 是区分两种病的唯一判据

- 缺口存在、但 `dirtyMarks` **一直是 0** ⇒ **"补不上"**：贴图到了，但 assembler 那帧已经因为
  `!texture.loaded` 提前 `return` 过，之后没有新脏标记就再也不会进来（见 boot.js 里
  `markSceneRenderDataDirty` 的注释）。
- `dirtyMarks` 增长过、且随后 `open` 归零 ⇒ **"后到"**：链路正常，只是慢。

两者的修法完全不同，此前没有任何日志能把它们分开。

### 落盘与手动探针

缺口上报会同时写进 `~/Library/Application Support/GameLobby/diagnostics.log`：

```
[render-gap] account=27黑阴.bin reason=gap-close open=0 total=17 worst=1420ms dirtyMarks=3 urls=a.pvr,b.pvr
```

⇒ **不用再从 Xcode 控制台挑行粘贴**。

新增**只读**手动探针（**无副作用，可趁"正缺着"反复按**）：

```js
window.__ios2RenderGapDump()
// → { installed:true, open:3, total:17, worstMs:1420, dirtyMarks:3,
//     openGaps:[{url:"de144869-….b1cc6.pvr", aliveMs:820}, …] }
```

> ⚠️ 与 `window.__ios2RenderIntegrityCheck()` 的区别：那个会 `scheduleASTCPVRRecovery()`
> + `markSceneRenderDataDirty()`，**当场把缺块补上**（`:1968-1971`），只能用来"看能不能自愈"，
> 不能用来"看清现状"。`__ios2RenderGapDump` 只读快照，专治这个。

## 14.4 下次复现该看什么（三步）

1. **`grep [render-gap] diagnostics.log`**
   - 有 `gap-close` 且 `worst=几百 ms~几秒` ⇒ **确认"先缺后全"**，看 `urls` 是哪几张图。
   - `dirtyMarks=0` 而缺口存在 ⇒ 转**"补不上"**分支，重心移到脏标记/assembler。
   - **一条都没有** ⇒ 缺的不是 `cc.Sprite` 贴图，落进 Label/Spine/Graphics 盲区（§12.4）。
2. **`grep "waiting for shared download" `** —— 数同一 URL 的 `by=` 个数，
   直接看出"几个实例在等同一份下载、谁等得最久"。
3. **`grep "webcontent pid"`** —— 拿到 PID→账号映射后，RBS 断言 / `WebContent[...]` 那批噪音
   终于能归位到具体窗口。

## 14.5 未做（留待下一轮）

- **Label / Skeleton / Graphics 仍未纳入账本**：它们本来就不产生 `spriteFrame` 组件，
  若第 1 步"一条 `[render-gap]` 都没有"，就说明缺的正在这三个里——那时再加。
- **PID 是尽力而为**：`_webProcessIdentifier` 是私有 getter，用 `responds(to:)` 先探再取，
  KVC 取未定义 key 会抛 ObjC 异常（Swift 接不住、直接崩），所以探不到就打 `?`，**不用启动顺序猜**。

---

# 15. 第四次实测（`/Users/gg/log.txt`，buildTag 2026-09-20.33）

> 现象：`破阴.bin` 与 `混阴.bin` 出现画面缺失。
> 5358 行 / 675 KB。**这次跑的就是加上观测的那一版**（`build 2026-09-20.33`）。

## 15.1 新加的观测：全部生效 ✅

| 新标记 | 条数 | 说明 |
|---|---|---|
| `[instance] <账号> webcontent pid=` | **7** | 7 个实例全打出来了，PID↔窗口从此对得上 |
| `[scheme] <账号> bundle URL rewritten` | 678 | 归属成功 |
| `[cdn] … (by=<账号>)` | 658 | 归属成功 |
| `[cdn] waiting for shared download … (by=…)` | **304** | "谁在等同一份下载"第一次可读 |
| `[render-gap]` / `[instance] render gap` | **0** | ⚠️ 见 §15.3 |

**PID 映射（本轮）**：

```
21电阴.bin pid=8433   22红血.bin pid=8434   23甜阴.bin pid=8439   24潮阴.bin pid=8440
25破阴.bin pid=8437   26混阴.bin pid=8438   27黑阴.bin pid=8441
```

## 15.2 这一轮到底发生了什么：一次点击 → 7 窗同开「答题」，7 窗抢同一批资源

**177 次网络下载，分布在 `quiz` / `ui_quiz` / `ui_quiz_common` / `icons` 四个 bundle 上。**

### (a) 三张 2048² × 1.4 MB 立绘：1 个下载、6 个等

`ui_quiz` 里的三张大图，每个 URL 的归属链**完全一致地**是「1 + 6」：

| 贴图 | 下载者 | 等待者（6 个） |
|---|---|---|
| `cd0e9d55-…b7291.pvr` | 23甜阴 | 22红血 / 21电阴 / 24潮阴 / **25破阴** / 27黑阴 / **26混阴** |
| `d2cc9bfc-…a1025.pvr` | 24潮阴 | 21电阴 / 27黑阴 / 22红血 / 23甜阴 / **25破阴** / **26混阴** |
| `e934be39-…1bded.pvr` | **26混阴** | 27黑阴 / 21电阴 / 22红血 / **25破阴** / 24潮阴 / 23甜阴 |

尺寸 `1400848 B = 16 + 256 × 342 × 16` ⇒ **2048×2048、ASTC 8x6**。
**7 个实例全都要这三张图，但网络上只发一次**——另外 6 个实例各阻塞一次。

### (b) 等待成本排名（这是本次最有信息量的一张表）

| 账号 | 发起下载 | 共享等待 |
|---|---:|---:|
| **25破阴.bin** | 41 | **61 ← 最高** |
| 21电阴.bin | 61 | 57 |
| 27黑阴.bin | 8 | 49 |
| 22红血.bin | 7 | 38 |
| **26混阴.bin** | 32 | 35 |
| 24潮阴.bin | 6 | 33 |
| 23甜阴.bin | 22 | 31 |

**`25破阴` 是全场等待最多的实例（61 次）**，与用户报的缺块窗口对得上。

`26混阴` 等待 35 次（第 5），但它有个独特之处：**它是 `e934be39`（1.4 MB）的唯一下载者**——
这一张要它自己拉、自己解析、自己上传，其余 6 个只是等。

`26混阴` 发起 32 次下载里 **21 次是 `icons`**（头像图标），也可能与"头像位空白"的观感有关。

### (c) 请求量本身是均匀的

各账号的 `[scheme]` 请求数：红血 86 / 混阴 86 / 潮阴 85 / 破阴 85 / 电阴 84 / 甜阴 83 / 黑阴 81
—— **7 个窗口请求的 bundle 集合几乎完全一样**（84±5）。这不是某个窗口"多开了界面"，
而是**同一次点击被 7 份回放，7 份各自去要同一批资源**。

## 15.3 ⚠️ 本轮最大的问题：`[render-gap]` 一条都没有，而且我**判断不了**为什么

`[render-gap]` = 0 只有两种可能：

| 可能 | 含义 |
|---|---|
| **A. 账本没装上** | `cc.Sprite` 那一刻还没定义 / 钩子方法名不对 → 等于没观测 |
| **B. 装上了，但确实没有 `cc.Sprite` 贴图缺口** | 缺的是 **Label 文字 / Spine / Graphics**，即 §12.4 的盲区 |

**而这一轮的日志无法区分 A 和 B**，因为账本安装成功时我打的是
`console.log('[ios2-web] render gap ledger installed')` —— 它经 console 桥变成
**debug 级**，而 `LobbyLog.minimumLevel` 默认是 **info**，**直接被丢掉**。
（证据：整份日志里 `[ios2-web] boot revision` / `[ios2-web] target frame rate` / `PVR texture loaded`
全是 **0** 条，说明页面侧 `console.log` 根本没进日志。）

> **教训**：诊断工具**自己**不可观测，就等于没有诊断。
> 这是这一轮暴露出来的真问题，比"缺块本身"更值得先修——否则下一轮还是同样卡在这里。

## 15.4 本轮已排除的（多了一层证据）

| 项 | 条数 |
|---|---|
| `[scheme]` 告警/错误（pending / abandoned / CDN error） | **0** |
| `[sync]` 丢事件（`miss`） | **0** |
| `webgl-lost` / `webgl-fatal` | **0** |
| `texture-upload-failed` | **0** |
| `slow main work` | 1（启动期 ready 那条） |

⇒ **没有任何请求失败或超时，同步也没丢事件**。整份日志从宿主角度看是"一次成功会话"，
唯一的成本就是 §15.2(b) 那张等待表。

## 15.5 立刻修掉（buildTag `2026-09-20.34`，同轮完成）

1. **账本自述**：安装时上报 `reason: 'ledger-installed'`（带上实际包上的钩子列表）或
   `reason: 'ledger-unavailable'`（带上原因），**走 `render-gap` 通道 → 一定到得了原生并落盘**。
   从此"没缺"与"没装上"不再混淆。
2. **补上 Label / Skeleton 覆盖**（本轮证据指向文字的可能性很高）：
   - `cc.Label._validateRender`：引擎原文见下，BMFont 图集或 `.fnt` 配置没到同样调 `disableRender()`
     ```js
     if (this.string) { if (this._materials[0]) { var t = this.font;
       if (!(t instanceof cc.BitmapFont)) return;
       var e = t.spriteFrame;
       if (e && e.textureLoaded() && t._fntConfig) return; } this.disableRender(); } else this.disableRender();
     ```
   - `sp.Skeleton._validateRender`：`skeletonData.isTexturesLoaded()` 为假时同样静默。
3. **新增 `kinds` 字段**（`sprite-texture` / `font-atlas` / `font-config` / `skeleton-texture`）
   —— 一眼看出缺的是图还是字。
4. **账本心跳**：每 5s 一次 `pruneGapLedger()`（健康时是空转）。
   必须有的原因：`.fnt` 配置这类缺口**没有贴图可挂 `load` 监听**，只靠事件销账的话
   `openCount` 会永远卡在 >0；心跳负责复查销账。
5. `cc` 上找不到钩子点时**不再静默 return**，而是上报 `ledger-unavailable` + 原因。

## 15.6 下一轮该看什么

```bash
grep "[render-gap-ledger]" ~/Library/Application\ Support/GameLobby/diagnostics.log   # ① 装没装上
grep "\[render-gap\]"      ~/Library/Application\ Support/GameLobby/diagnostics.log   # ② 缺口账本
```

- ① 出现 `ledger-installed … Sprite._applySpriteFrame+Sprite._validateRender+Label._validateRender+…`
  ⇒ 覆盖面确认，②有任何一条就是真缺口，`kinds=` 直接告诉你是图还是字。
- ① 出现 `ledger-unavailable …` ⇒ 钩子没挂上，按它给的原因修（这才是真 bug）。
- ① 有、② 没有 ⇒ **确认为"零 Sprite/Label 缺口"**，那么缺的只可能在
  `cc.Graphics` / FGUI 自绘 / 粒子 里，范围一次缩小到底。

**也可以当场手验**（Web 检查器已开）：

```js
window.__ios2RenderGapDump()
// { installed:true, hooks:[...], open:3, total:17, worstMs:1420, kinds:"sprite-texture", dirtyMarks:3, openGaps:[…] }
```

---

# 16. 第五次实测（`/Users/gg/log3.txt`，buildTag 2026-09-20.34）——**找到了"只有部分窗口"的真正原因**

> 现象：`红血.bin` 与 `混阴.bin` 出现画面缺失。
> 3331 行。**这次跑的是带账本自述的那一版**。

## 16.1 账本自述到位：**装上了，而且缺口确实是零**

7 个实例各一条，也落盘到了 `diagnostics.log`：

```
[render-gap-ledger] account=22红血.bin ledger-installed Sprite._applySpriteFrame+Sprite._validateRender+Label._validateRender
…（共 7 条，全部 ledger-installed）
```

**两个结论：**

1. ✅ **账本确认装上了**（§15.3 的"可能 A"排除）。
2. ✅ **`[render-gap]` = 0，且日志与落盘文件都没有** ⇒ **缺的既不是 `cc.Sprite` 贴图，也不是 `cc.Label` 的 BMFont 图集/配置**。

> 附注：钩子列表里**没有 `Skeleton._validateRender`** ⇒ 这个版本里 `cc.Skeleton` 不存在（或没有该方法）。
> 本轮已补 `Mask._validateRender`（图片模板，引擎里同样静默）。

## 16.2 🎯 真正的发现：**同一次点击，不同窗口落到了不同的 UI**

日志最末尾（行 3118-3125）——同一次同步点击之后：

```
3118  25破阴.bin   ui_club_war_lottery     ┐
3119  21电阴.bin   ui_club_war_lottery     │
3120  23甜阴.bin   ui_club_war_lottery     │ 5 个窗口 → 军团战抽奖
3121  27黑阴.bin   ui_club_war_lottery     │
3124  24潮阴.bin   ui_club_war_lottery     ┘
3122  22红血.bin   ui_mail                 ┐ 2 个窗口 → 邮件
3125  26混阴.bin   ui_mail                 ┘
```

**而用户报缺块的窗口，正是 `22红血` 与 `26混阴` —— 走"少数分支"的那两个。**

### 与资源链完全吻合

整份日志**只有三次网络下载**，全在 `icons` 包里，且**只在 红血 ↔ 混阴 之间互等**：

```
3101  icons/import/71/71dee6c9-….22bd7.json (199 B)  下载=22红血   等待=26混阴
3108  icons/import/ae/aee3f628-….f0473.json ( 82 B)  下载=26混阴   等待=22红血
3111  icons/native/ae/aee3f628-….a7871.pvr (7088 B)  下载=26混阴   等待=22红血
```

**全场只有两个账号卷进了新资源下载，正好就是报缺块的那两个。**
其余 5 个窗口进的 `ui_club_war_lottery` 资源命中缓存 → 无感。

### 机制

同步回放的坐标**确实做了归一化**（`InputSyncController`：`clientX / window.innerWidth` → 0..1，
回放时换算回本窗口绝对像素），设计意图是"多开尺寸不一也不会错位"。

**但这个前提是"各窗口的 UI 布局随宽高比等比变化"**。实测不成立：
2+1 网格里格子宽高比不同，而游戏的 UI 适配（`Widget` 自适应、以及游戏自己设定的
设计分辨率/适配策略——会覆盖我们在 `multiOpen` 下设的 `EXACT_FIT`）
让固定按钮在**归一化坐标**下落在不同位置。

> ⇒ **同一个 (x, y) 归一化位置，在 A 窗口是「军团战抽奖」按钮，在 B 窗口是「邮件」按钮。**
> 于是"一次点击 7 个窗口做同一件事"变成了 5 + 2；那 2 个窗口进了**没被其它窗口预热过**的
> `ui_mail`，它引用的 `icons` 包尚未缓存 → 现下载 → 面板先出、图标后到。
> **这就是"部分窗口出现画面缺失"——不是随机，是命中率差异。**

> 这也解释了为何前三次都卡住：我们一直在找"哪个渲染路径静默了"，
> 而根因在**上一层**——那 2 个窗口显示的根本是**另一个界面**。

## 16.3 本轮新增的两个决定性观测（buildTag `2026-09-20.35`）

### ① 视口指纹 `[instance] WebGL viewport: …`

每实例打两条（引擎初始化后 / 8s 后各一条），带账号：

```
reason=settled win=960x540 canvasAttr=1920x1080 canvasCSS=960x540 dpr=2
frame=960x540 visible=… design=… policy=…
```

**用途**：坐实"窗口尺寸/设计分辨率 → 布局"这一环。若各窗口的 `visible/design/policy` 确实不同，
则 §16.2 的机制被彻底证明；若完全相同，说明差异来自游戏 UI 自身（那就是"点击元素不同"以外的解释，
需要回头看 §16.4）。

### ② 类无关审计 `window.__ios2RenderAudit()`（只读，无副作用）

**不问是谁调的 `disableRender()`，直接查结果** —— `node._renderFlag` 少了 `FLAG_RENDER` 位
就是"本该画但被关掉了"。引擎里 `disableRender()` 散落在
`Sprite` / `Label` / `Mask` / `Skeleton` / `ParticleSystem` / `TiledLayer` / `ArmatureDisplay` …
每一处都静默；查这个位就与"谁干的"无关了。

返回：

```js
{
  ok: true, scanned: 1284, withRenderer: 903,
  classes: { Sprite: 610, Label: 240, Graphics: 41, Mask: 12 },   // ← UI 到底由什么构成
  silencedByClass: { Sprite: 0 },                                  // ← 谁真的被关掉了
  silenced: [ { node, comp, textureState, url }, … ]
}
```

**`classes` 直方图这一项最有价值**：它一次性回答"这套 UI 到底有多少 `cc.Graphics` /
`cc.Label` / 其它类"，从而决定还有哪些类**必须**挂钩 —— 不必再猜。

## 16.4 下一轮怎么收口

1. **复现缺块时，在缺的那个窗口 Console 执行**（Web 检查器已开）：
   ```js
   window.__ios2RenderAudit()
   ```
   - `silenced.length > 0` ⇒ **拿到了**：`silenced[].comp` 告诉你是哪个类、`textureState` 告诉你是
     "贴图没到"还是"根本没贴图"、`node` 告诉你是哪个节点。
   - `silenced.length === 0` 且画面确实缺 ⇒ **那不是"渲染被关掉"**，而是**部件压根没创建**
     （例如 FairyGUI 的异步加载器在资源到达前不创建显示对象）——方向转为查 FGUI 的加载回调。
2. **对照 7 个实例的 `viewport` 指纹**，确认 §16.2 的机制。
3. 若确认坐标分歧：**同步侧要按"设计分辨率/可见尺寸"归一化，而不是按 `window.innerWidth/innerHeight`**
   —— 现在是"窗口归一化"，需要改成"游戏视口归一化"（或直接广播 canvas 相对坐标 + 各窗口自己的
   `visible/design` 换算）。这是**下一个明确的、可实施的修复点**。

## 16.5 结合用户补充信息收口：**缺的是「主界面上的图标/按钮图」**

用户确认：**缺的是图标/按钮图，位置在主界面/大厅**。结合本地 CDN 缓存，链条完全闭合：

### `icons` 包是什么

```
缓存里 icons 条目 2044 条：json 1428 / pvr 606 / png 7 / js 2
606 张 pvr 的像素尺寸：80x80 ×187 / 204x100 ×50 / 120x120 ×48 / 202x115 ×34 /
                      183x118 ×32 / 180x180 ×31 / 185x190 ×26 / 140x140 ×22 …
合计 606 张 pvr = 6.5 MB；整个 icons 包（含 json）17.4 MB
```

⇒ **`icons` 是「一堆互相独立的小图」，不是图集**：`80×80` 那 187 张是头像，`~200×100` 那 100 多张
是按钮图 / 称号 / 徽章。**按需单独加载，走持久缓存**。

### 为什么只有那两个窗口

本轮全场**只有 3 次网络下载**，全在 `icons`，且只在 红血 ↔ 混阴 之间互等：

```
3101  icons/import/71/71dee6c9-….22bd7.json (199 B)  ← SpriteFrame 元数据   下载=红血 等=混阴
3108  icons/import/ae/aee3f628-….f0473.json ( 82 B)  ← Texture2D 元数据     下载=混阴 等=红血
3111  icons/native/ae/aee3f628-….a7871.pvr (7088 B)  ← 那张图本体           下载=混阴 等=红血
```

**其余 5 个窗口需要的图标早已在持久缓存里**（缓存已有 606 张 pvr / 6.5 MB），
只有这两个窗口当时需要的那几张是新的 ⇒ **只有它们缺**。这正是"部分窗口"的由来——
**不是随机，是"这张图在不在缓存里"的差异。**

> 推论（可验证）：**同一账号第二次进主界面就不会再缺**——缓存已经暖了。

### ⚠️ 关键修正：这不是"渲染被静默"

账本自述 7/7 `ledger-installed`，`[render-gap]` **0 条** ⇒ **没有出现"贴图未就绪但已赋给 `cc.Sprite`"的窗口**。
按"主界面 + 图标"这个组合，最合理的解释是：

> 游戏是在**资源加载完成的回调里**才把 `spriteFrame` 赋给节点（或那时才创建节点）。
> 于是"缺"的形态是 **「还没赋 / 还没建」，而不是「赋了但画不出来」**——
> 后者才会走到引擎的 `disableRender()`，前者**根本不存在可挂钩的时刻**。

**⇒ 修法不在渲染层。** 这不是一个"引擎静默路径"的 bug，而是**资源到达前 UI 没有占位**。

### 可选的修法（按性价比）

| 方案 | 成本 | 效果 |
|---|---|---|
| **A. 预热 `icons` 全量** | 一次 17.4 MB 网络（之后全走缓存） | 彻底消除主界面图标后到；代价是启动/背景多 17 MB |
| **B. 预热"本账号主界面用到的图标子集"** | 小 | 精准，但要先摸清依赖集合（`ui_main` / `ui_home` 的 config 里能推） |
| **C. 页面侧给图片位加占位图/骨架** | 中（不改游戏脚本的话要在垫片层做，较难） | 观感最好，但工作量大 |
| **D. 什么都不做** | 0 | 缓存暖后自然消失（只影响"第一次遇到某张图"） |

> 建议先做 **B**（或退一步 A），这是**当前唯一能真正消除"主界面图标缺失"的动作**。

### 顺带查到的一个独立缺陷（建议单独修）

日志末尾 3118-3125：**同一次同步点击，5 个窗口进了 `ui_club_war_lottery`，另外 2 个进了 `ui_mail`**。
群控坐标是**窗口归一化**（`InputSyncController`：`clientX / window.innerWidth` → 0..1），
回放时换算回本窗口绝对像素。前提是"各窗口 UI 布局随宽高比等比"，**实测不成立**
（2+1 网格格子比例不同；游戏自己的适配策略会覆盖我们在 `multiOpen` 下设的 `EXACT_FIT`）。

⇒ **同一个归一化坐标在不同窗口是不同按钮**。这与本次图标缺失是**两件事**（下载发生在点击之前），
但它是个真缺陷：**群控会"点错按钮"**，应当改成按**游戏视口**（canvas / `visibleSize` / `designResolution`）
归一化，而不是按 `window.innerWidth/innerHeight`。诊断依据用新增的
`[instance] WebGL viewport: reason=settled win=… visible=… design=… policy=… (<账号>)`。

---

# 17. 第六次实测（`/Users/gg/log4.txt`，buildTag 2026-09-20.35）

> 现象：`混阴.bin` 从大厅进入**玩具**窗口，玩具窗口**没有加载完整**，
> **并且大厅上也出现了部分玩具的页面**。6974 行。

## 17.1 先纠正 §16.2 的一个结论：**7 个窗口尺寸完全一样**

视口指纹这次采到了（14 条 = 7 实例 × 2）：

```
reason=engine-init win=296x526 canvasAttr=300x150 canvasCSS=296x148 dpr=2 (×7)
reason=settled     win=296x526 canvasAttr=444x789 canvasCSS=296x526 dpr=2 (×7)
```

**7 个窗口 `win` / `canvas` **逐个相同**（296×526，画布 444×789，dpr=2）。**

⇒ **§16.2 里"窗口宽高比不同 → 同一个归一化坐标落到不同按钮"这个机制站不住**——
窗口尺寸是一样的，设计空间换算也就一样。

正确的解释只能是：**各实例的 UI 状态不同步**（加载/动画/面板栈进度不同），
而同步点击是**无状态广播**的——它只带坐标，不带"你现在该在哪个界面"。
某个窗口还停在上一屏 / 动画中途时，同一次点击就会落到不同的元素上。

> ⚠️ 另外：`frame` / `visible` / `design` / `policy` 这次**全是 `?`** ——
> 是我把 `cc.view.getFrameSize` 当裸函数调用了（丢了 `this`）。**已在 `.36` 修掉**
> （改成闭包绑定），下一轮这四个值才真正可用。

## 17.2 混阴 的尾部时间线

```
6755/6756   最后一次 [sync] publish（破阴 发起）——之后再无广播
6778        25破阴.bin 焦点=off → ok-restart:15   ┐ 焦点从破阴切到混阴
6779        26混阴.bin 焦点=on  → ok-restart:30   ┘ 两个实例主循环都拆了重建
6824-6963   24 次全量扫描，[sync] publish = 0 条   ← 全在混阴里直接操作（未广播）
6964        [scheme] 26混阴.bin bundle URL rewritten: /remote/xiaobing3_jian/index.70599.js
            ★ 玩具 bundle 开始加载 —— 并且「日志到此结束」
```

**全场只有 `26混阴` 请求过 `xiaobing3_jian`（玩具/兵种包）**，其余 6 个窗口没有。
⇒ 这次是**在混阴里单独点的**，没有通过同步广播出去（与"6756 之后再无 publish"一致）。

其余排除项本轮依旧干净：`[scheme]` 告警 **0**、`[render-gap]` **0**、
`webgl-*` / `texture-upload-failed` **0**、4600 行之后 `[cdn]` 下载 **0**。

## 17.3 本轮暴露的观测缺口：**看不出"bundle 加载完整了没有"**

日志里这块是空的：

- `[scheme] bundle URL rewritten` **只记 `index.<ver>.js` 那一条**（因为只有它会被改写版本号）；
- 同一个 bundle 的 `config.<ver>.json` 与 `native/*` 请求**不走那条日志**；
- `[cdn]` **只记"网络下载"**，**缓存命中完全不记**。

⇒ 对 `xiaobing3_jian`，我们只能看到"index.js 被请求了"，**看不到后面还有没有 config / native、
也看不到到底成功没有**。"没加载完整"这件事，恰好落在日志的盲区里。

## 17.4 已修（buildTag `2026-09-20.36`）

| 改了什么 | 为什么 |
|---|---|
| **修 `reportViewportFingerprint` 的 `this` 丢失** | 上一轮 `frame/visible/design/policy` 全废成 `?` |
| **`[scheme] <账号> bundle requests in 60s: total=N <bundle>=n …`** | 补齐 §17.3 的盲区。ⓐ"某个 bundle 只请求了 1 个文件就没了" = 没加载完整；ⓑ 正常界面会看到该 bundle 十几次请求。每实例每 60s 一条，噪音可忽略 |
| **`__ios2RenderAudit()` 加 `outline`（子树轮廓）与 `textureState`** | 见 §17.5 |

## 17.5 下次复现：**在缺的那个窗口按一下 `window.__ios2RenderAudit()`**

它现在能一次区分三种"缺"：

| 观察 | 结论 |
|---|---|
| `silenced[].textureState === 'no-texture'` | **节点建了、但压根没赋上图 / 字体** ⇒ 这就是**"没加载完整"**（不是贴图后到） |
| `silenced[].textureState === 'not-loaded'` | 图已赋、贴图没到（"后到"） |
| `silenced.length === 0` 而画面确实缺 | 这些部件**根本没被创建**（FGUI 异步加载器在资源到达前不建对象） |
| `outline` 里**同时出现大厅与玩具两棵子树** | **层叠被坐实**——"大厅上叠了部分玩具页面"就是它 |

`outline` 每项是 `{depth, node, children, renderers, off}`：
`renderers>0 && off===renderers` = 这棵子树整个没在画。

## 17.6 一句结论

本轮**没有拿到失败证据**——日志在玩具 bundle 开始加载的那一行就结束了，
而 `[scheme]`/`[cdn]`/缺口账本三条通道对"bundle 加载到什么程度"**本来就是盲的**（§17.3）。
`.36` 把这三处补上之后，下一轮才有判据。

> 另外，`混阴` 在 6779 有过一次**主循环真重建**（`pause()` + ≥120 ms 空窗，来自焦点切换），
> 若玩具的打开恰好落在那个窗口里，主循环被暂停本身就足以让一次界面过渡卡在半途。
> 这一点目前**只是候选**：日志只能证明"重建发生在 185 行之前"，证不了重叠。
> `.36` 的 bundle 汇总能把它俩对上时间。

---

# 18. 取证入口：从「手敲命令」改成「按一下快捷键」（buildTag 2026-09-20.37）

## 18.1 先回答：`window.__ios2RenderAudit()` 是什么、为什么"看不见"

**它是一段跑在游戏页面（WebView 内部）的 JavaScript 函数**，不是文件、不是按钮、也不在日志里。
只有在**页面的控制台**里敲它，它才会运行并返回一个对象（渲染组件清单 / 被静默的构件 / 子树轮廓）。

之前几轮日志里出现的 `[render-gap]`、`[render-gap-ledger]` 是**它这类探针落盘后的结果**，
不是探针本身——所以"在日志里见过、但界面上找不到"，是正常的。

**原来的按法**（需要 Web 检查器，而缺块只有几百毫秒，等敲完命令早自愈了）：

1. 设置页 → 「Web 检查器」打开（本机已经是 on）
2. **在缺块的那个游戏窗口上右键 → 检查元素**
3. 切到 Console，粘 `window.__ios2RenderAudit()`，回车

## 18.2 现在：菜单栏一个快捷键就够（**.37** 新增）

```
1. 先点一下出问题的那个窗口     ← 让它成为「焦点实例」
2. 菜单栏「诊断」→「抓取渲染诊断快照」   （快捷键 ⇧⌘A）
3. 打开 ~/Library/Application Support/GameLobby/diagnostics.log
   看 [audit] 开头的行
```

**不用开 Web 检查器，也不用粘任何命令。**

### 它一次抓三样

| 内容 | 用来回答 |
|---|---|
| `__ios2RenderAudit()` | 哪些构件被引擎静默了（**类无关**）+ 渲染组件类直方图 + **子树轮廓** |
| `__ios2RenderGapDump()` | 缺口账本：现在缺几个 / 缺了多久 / 缺的是哪几张 |
| 视口指纹 | 顺带补一条 `[instance] WebGL viewport`（窗口 / 画布 / visible / design / policy） |

落盘形态：

```
[audit] account=26混阴.bin reason=hotkey {"audit":{"ok":true,"scanned":1284,"withRenderer":903,
        "classes":{"Sprite":610,"Label":240,"Graphics":41},"silencedByClass":{"Sprite":0},
        "outline":[{"depth":1,"node":"Canvas","children":3,"renderers":0,"off":0}, …]},
        "gaps":{"installed":true,"open":0,"total":17,"worstMs":1420,"kinds":"","dirtyMarks":3}, …}
```

### 设计取舍（写下来免得以后改错）

- **只抓焦点实例**：缺块是**单个窗口**的现象，用户看到问题时会先点那个窗口（它就成了焦点）；
  一次抓 7 个不但噪音大，还会把真正有问题的那条淹掉。无焦点时才抓全部。
- **单条封顶 6000 字符**：账本文件是"取证卡片"，不该被一次快照挤爆（`DiagnosticsLog` 超 256 KB 只留尾部）。
- `boot.js` 的 `outlineLimit` 由 80 降到 **40**，给上面的封顶留余量。

---

# 19. 第七次实测（`/Users/gg/log5.txt`，buildTag 2026-09-20.37）——审计触发了，但**返回 nil（我的 bug）**

> 现象：`混阴.bin` 画面缺失；用户按了「抓取渲染诊断快照」。4188 行。

## 19.1 🔴 我的错误：`callAsyncJavaScript` 的入参是**函数体**，必须顶层 `return`

日志与落盘都收到了：

```
[instance] audit snapshot: 26混阴.bin nil
[23:53:25.280] [audit] account=26混阴.bin reason=hotkey nil
```

**`nil` 不是页面报错，是那段 JS 没交回值。** 原因是第一版把它整段包进了 IIFE：

```js
(function () { … return JSON.stringify(out); })()     // ← 表达式语句，值被丢弃
```

而 `callAsyncJavaScript` 把这个字符串当作**函数体**（等价于 `async function () { <body> }`），
**只有顶层 `return` 才会作为结果交回**。对照：`frameRateApplyScript`（帧率那段）一直是顶层
`return 'ok-restart:' + fps;`，所以它从来没出过问题。

**已在 `.38` 修**，并且加了一发最小探针：

```
[instance] audit ping: <账号> pong:<ts>      ← 通道通不通，一行就知道
```

下次若还拿不到值，就能立刻区分「通道不通」与「那段脚本自己没返回值」，不必再耗一轮。

## 19.2 ✅ 但另外两项新观测**都生效了**，而且给出了硬数据

### (a) 视口指纹修好了，并**推导出游戏的适配策略**

（上一轮 `frame/visible/design/policy` 全是 `?`，是我把 `cc.view.getFrameSize` 当裸函数调用丢了 `this`；已修）

```
engine-init  win=296x526  frame=296x526  visible=300x150   design=300x150
settled      win=296x526  frame=296x526  visible=750x1333  design=750x1334
audit(混阴)  win=330x586  frame=330x586  visible=750x1332  design=750x1334
```

**设计分辨率 `750x1334` 固定，宽度 750 不变，高度随窗口比例浮动：**

| 窗口 | 实测 visible.h | 按 `750 × winH/winW` 推算 |
|---|---|---|
| 296×526 | **1333** | 750 × 526/296 = **1332.77 → 1333** ✅ |
| 330×586 | **1332** | 750 × 586/330 = **1331.82 → 1332** ✅ |

⇒ **适配策略 = 固定宽度（`FIXED_WIDTH`）**，不是我在 `multiOpen` 下设的 `EXACT_FIT`
（说明游戏自己又调了 `setDesignResolutionSize`）。`.38` 里已把这个判定直接算出来打进日志
（新增 `fit=`、`aspect=`）。

**意义**：固定宽度下，设计空间的 y 映射是 `y_design = normalized_y × 750 × (winH/winW)`
—— **窗口比例不同 ⇒ 同一个归一化 y 落到不同设计位置**。这是一条**确实存在**的偏差通道。
⚠️ 但本轮两个窗口的比例差只有 **0.07%**（1.7770 vs 1.7758），影响可忽略，
**不足以解释"5 个窗口 vs 2 个窗口"那种分叉**。分叉另有原因（见下）。

### (b) `bundle requests in 60s` 汇总：**首次量化了"各窗口 UI 状态分叉"**

同一时间段内，各 bundle 被几个账号请求过：

```
ui_nightmare            7   ← 7 个窗口都在
ui_battleScene          7
sdsl_boss2 / 5 / 6 / 7 / 8_1   7
ui_club_war_common_panels      6
ui_legionPayload_missions      6
sdsl_boss3              5
ui_teamUp               3
txk_sdsl9               3
sdsl_boss1              2     ← 只有 26混阴 / 27黑阴
txk_zhouyu8             2
xiaobing_kulou          1     ← 只有 27黑阴
guojia_ice              1     ← 只有 22红血
sdsl_boss4              1     ← 只有 25破阴
xiaobing2_qingtongniao  1
buff_silent             1
```

**7 → 6 → 5 → 3 → 2 → 1，逐级分叉。** 这就是"同一次点击在不同窗口落到不同界面"的**累积效应**：
每一次点击都要求"各窗口此刻处于同一状态"，而它们加载/动画/面板栈的进度并不一致，
于是每点一次就多分出一层。**这是第一次把这件事量化出来。**

> 换句话说：**"部分窗口画面异常"是分叉的结果，不是分叉的原因。**
> 真正要治的是**状态同步**（点击前把各窗口对齐，或让同步带上"目标界面"而不是裸坐标）。

## 19.3 末尾时间线（与用户操作对齐）

```
4165/4166  [sync] publish 26混阴 mousedown/mouseup → 5 目标   ← 用户在混阴里点了
4171       24潮阴.bin 焦点=off → ok-restart:15   ┐ 焦点 潮阴→混阴
4174       26混阴.bin 焦点=on  → ok-restart:30   ┘ 混阴主循环被拆了重建
4181       [session] audit snapshot requested: 1 instance(s)  ← 用户按了 ⇧⌘A
4182       26混阴.bin nil                                      ← 见 §19.1
4183       viewport: reason=audit win=330x586 visible=750x1332 design=750x1334
4187/4188  又按了一次，同样 nil
```

`26混阴` 在 4174 有一次**主循环真重建**（`pause()` + ≥120 ms 空窗，来自焦点切换）。
仍然只是"时间上相邻"，无法证明与缺块重叠——但**这是第三次**在报缺块的窗口上看到它。

## 19.4 下一步（`.38`）

**再做一次同样的操作**：让混阴出现缺失 → 点一下混阴 → 按 **⇧⌘A** → 把 `log5` 同款日志给我。

这次会多出两样东西：

1. `[instance] audit ping: … pong:<ts>` —— 通道自证
2. `[audit] account=26混阴.bin reason=hotkey {"ok":true,"audit":{…},"gaps":{…}}` —— **真正的审计数据**

拿到 ② 之后，§17.5 那张判定表（`textureState` 三态 + `outline` 是否两棵树）就能当场给出结论。

---

# 20. 第八次实测（`/Users/gg/log6.txt`，buildTag 2026-09-20.38）——**审计成功，直接命中根因**

> 现象：`红血.bin` 画面问题；用户按了诊断抓取。4602 行。`audit ping` 通了（`pong:1789920075309`）。

## 20.1 拿到的审计结果

```json
{"ok":true,"multiOpen":true,
 "audit":{"ok":true,"reason":"ok","scanned":1384,"withRenderer":368,
   "classes":{"CCClass":180,"Qi":132,"zi":56},
   "silencedByClass":{"Qi":31},
   "silenced":[{"node":"Image","comp":"Qi","textureState":"no-texture"} × 31]},
 "gaps":{"installed":true,
         "hooks":["Sprite._applySpriteFrame","Sprite._validateRender",
                  "Label._validateRender","Mask._validateRender"],
         "open":0,"total":0,"worstMs":0,"dirtyMarks":0,"openGaps":[]}}
```

**三条硬结论：**

1. **31 个渲染构件被引擎静默，全部是同一类（`Qi`），节点的 `textureState` 全是 `no-texture`。**
2. 节点名是 `Image`，`outline` 里能看到 `GImage` → `Image` 的父子关系 ⇒ **这是 FairyGUI 的图片构件**
   （FGUI 的 `GImage` 底下挂一个 `cc.Sprite`）。
3. **缺口账本整整一条都没记**（`total=0 / open=0 / dirtyMarks=0`），而 `hooks` 显示四个钩子都装上了。

## 20.2 🔴 我的设计漏洞，被 audit 当场抓出

`textureState: "no-texture"` = **节点建好了，但那个 `cc.Sprite` 压根没有 spriteFrame**。

而我第一版的 `noteRenderGap` 是这样写的：

```js
var texture = frame && typeof frame.getTexture === 'function' ? frame.getTexture() : null;
if (!texture || texture.loaded !== false) return;     // ← frame 为 null 时直接跳过
```

**要求"必须先有 texture 才记账"** —— 于是**「根本没有 spriteFrame」这一整类被我漏掉了**。
而引擎里它同样是被静默的：

```js
_validateRender: function () {
  var t = this._spriteFrame;
  (this._materials[0] && t && t.textureLoaded()) || this.disableRender();   // t 为 null → 照样 disableRender
}
```

> **这就解释了 §17 / §19 里那个矛盾**：「零缺口，但画面确实缺」。
> 不是缺口不存在，是**我的账本只统计了"贴着图但没到"，没统计"连图都没有"**。

## 20.3 已修（buildTag `2026-09-21.39`）

| 改动 | 说明 |
|---|---|
| **账本补 `sprite-empty` / `label-empty` / `font-atlas-empty`** | "节点在、素材还没赋"这一类现在会记账，`recheck` 复查销账（拿到 frame 就消失） |
| **记账键改成「组件身份」** | 原来用节点 uuid；FGUI 的 `Image` 节点上可能挂多个构件，用节点会串味 |
| **`gapUrls` 去重** | 31 个同类缺口会把 6 个展示名额占满，等于什么都没说 |
| **长期缺口降频** | 同一批缺口持续 > 60s ⇒ 上报从 5s 降到 60s 一次。防刷穿 `diagnostics.log`（256 KB 上限）；顺带把"缺了 3 秒"和"缺了 5 分钟"分开 |
| **`classifyRenderer()` 用 `instanceof` 反查真类名** | 引擎是压缩的，`constructor.name` 只给出 `Qi`/`zi`/`CCClass`（`.38` 实测就是这三个，完全读不出来）；`cc` 上的构造器是好的 |
| **`silenced[].node` 改成短路径 + `outline` 加 `parent`** | 光有 `Image` 这个名字，满树都是 `Image`，定位不到任何东西 |

## 20.4 顺带确认了上一轮的推导

`.38` 的日志里已经能看到新字段：

```
WebGL viewport: reason=audit win=296x526 aspect=1.7770 canvasAttr=444x789 canvasCSS=296x526
                dpr=2 frame=296x526 visible=750x1333 design=750x1334 fit=FIXED_WIDTH (22红血.bin)
```

⇒ **`FIXED_WIDTH` 坐实**（设计分辨率 750×1334 固定、宽度不变、高度随窗口比例浮动）。

## 20.5 红血 的时间线

```
4488   22红血.bin 焦点=on → ok-restart:30    ← 焦点切到红血，主循环拆了重建
4499/4512/4535/4544/4555/4586/4595   [sync] publish 22红血（红血是同步主控，用户一直在点）
4581   [instance] audit ping: 22红血.bin pong:1789920075309   ← 通道自证
4582   [instance] audit snapshot: 22红血.bin {…}               ← 拿到数据
4583   WebGL viewport … fit=FIXED_WIDTH
```

## 20.6 下一步：`.39` 跑一轮，**看 `worst=` 的数值**

`.39` 之后，同样操作会产生 `kind=sprite-empty` 的缺口记录，形如：

```
[render-gap] account=22红血.bin reason=gap-close open=0 total=31 worst=???ms kinds=sprite-empty urls=…
```

**`worst` 就是判据：**

| worst | 结论 | 对策 |
|---|---|---|
| 几百 ms | **正常的加载延迟**（自愈），只是观感 | 要治只能上占位图 / 预热 |
| 数秒 ~ 数十秒 | **卡住了**（不是慢） | 查 FGUI 图集的加载回调 / 失败路径 |
| 一直不闭合（每 60s 一条 `tick`，`gap-close` 永不出现） | **永久缺失** | 那是渲染或数据问题，不是加载 |

同时 `classifyRenderer` 会把 `Qi` 解成真类名、`silenced[].node` 会给出 `A/B/C` 路径
—— 下一份审计能直接说出"**是哪个界面的哪些构件**"，不必再靠 `Image` 猜。

---

# 21. 第九次实测（`/Users/gg/log7.txt`，buildTag 2026-09-21.39）——账本开火了，但**口径错了 500 倍**

> 现象：`破阴.bin` 画面不全 + 抓取诊断。3874 行。用户按了 5 次（`audit ping` 5 条）。

## 21.1 账本终于出数了 —— 可数字明显不对

```
195   22红血  (gap-open) open=1  total=1  worst=0ms     kinds=sprite-empty          urls=<sprite-empty>@content
659   21电阴  (gap-open) open=37 total=77 worst=5539ms  kinds=label-empty+sprite-empty  urls=<label-empty>@RICHTEXT_CHILD,<label-empty>@GTextField,<sprite-empty>@Image
1565  22红血  (tick)     open=148 total=362 worst=18181ms
3206  22红血  (tick)     open=501 total=1506 worst=57774ms
3796  25破阴  (tick)     open=511 total=1583 worst=57598ms
```

- **`gap-close` 一条都没有**（账本从来没有"好了"过）
- **`open` 单调爬到 511 —— 正好是我设的 `GAP_LEDGER_LIMIT`（512）撞上限**
- `worst` 从 0ms 一路涨到 **57868ms（58 秒）**
- `dirtyMarks` **恒为 0**

## 21.2 而审计说的是另一回事 —— 差 500 倍

`.39` 的审计（`classifyRenderer` + `nodePath` 都生效了）对 **破阴**：

```json
{"scanned":413,"withRenderer":151,
 "classes":{"Sprite":89,"Label":35,"Mask":7,"CCClass":17,"Graphics":3},
 "silencedByClass":{"Sprite":1},
 "silenced":[{"node":"Container/GLoader/Image","comp":"Sprite","textureState":"no-texture"}]}
```

**引擎真正没画的构件：1 个。** 而账本同一时刻报 `open=511`。

> **两个探针互相打架，而这正是我上一轮特意设计来互补的**——结果把矛盾暴露出来了，是好事。

## 21.3 错在哪：`.39` 是"各分支各判条件"

`.39` 的钩子各自判断"这个分支看起来像缺口吗"：

- `Label._validateRender` 的钩子写的是 `!font → label-empty`；
- **但 TTF 系统字体的 `cc.Label`，`font` 本来就是 `null`**（引擎原文是
  `if (!(t instanceof cc.BitmapFont)) return;` —— **TTF 直接放行，根本不会 `disableRender()`**）。

⇒ 那 35 个 `Label` 全部被误记成缺口，而且 `recheck`（`!component.font`）**永远为真**，
于是**永不销账、无限累积**，20 秒就把账本顶到上限 511。

**教训**：账本不该复刻引擎的分支条件——**引擎的分支是会写错的，而它的最终裁决不会。**

## 21.4 已修（buildTag `2026-09-21.40`）

| 改动 | 说明 |
|---|---|
| **判定依据换成 `node._renderFlag & FLAG_RENDER`** | 引擎的最终裁决，**与 audit 用的是同一个信号**；与"谁调的 `disableRender()`、分支条件怎么写"完全解耦。挂钩子只负责提供一个**时机** |
| `describeGapAsset()` | 只在"已确认没画"之后负责贴标签（kind + 相关贴图），不再参与判定 |
| **`kinds` 带计数**（`sprite-empty=1,label-blocked=3`） | 只报名字的话，"两类都有"和"1 个真的 + 500 个误报"看起来一样。**主次必须一眼看得出来** |
| 新增 kind | `sprite-blocked`（图已 loaded 却没画）、`label-blocked`、`skeleton-blocked`、`blocked-<真类名>` |
| 删掉 `noteGap` / `noteRenderGap` / `noteLabelGap` / `noteMaskGap` / `noteSkeletonGap` | 归一成 `noteRenderFlagGap` 一个入口 |

## 21.5 ⚠️ 诚实修正一条我之前说错的

**`dirtyMarks` 至今恒为 0** —— 我在 §14.3 把它当作「区分"后到"与"补不上"的唯一判据」是**错的**。
`markSceneRenderDataDirty()` 只在 `reportRenderIntegrity()` 发现缺失时才被调用，
而那个 12s 看门狗从来没报过 ⇒ `dirtyMarks` 自然一直是 0，**它不区分任何东西**。
**只有在它 > 0 时才有信息量**（那时说明有人主动补过脏标记）。

## 21.6 破阴的根因（审计已经给出，且可信）

```
silenced = [{"node":"Container/GLoader/Image","comp":"Sprite","textureState":"no-texture"}]
```

**`Container/GLoader` 是 FairyGUI 的 `GLoader`（动态资源加载器）**——
它就是 FGUI 用来**按需加载外部图**的构件（头像、道具图、活动图这类）。
**`GLoader` 在资源到位之前，它的 `Image` 子节点就是一个没有 `spriteFrame` 的空 `cc.Sprite`。**

⇒ 与 §16.5 的 `icons`（606 张按需小图）**是同一条链**：
`GLoader` 去拉某张图标 → 还没回来 → 那个位置空白 → 用户看到"画面不全"。

## 21.7 下一步：`.40` 再跑一次，**只看一个数字**

```
[render-gap] account=25破阴.bin reason=gap-close open=0 total=1 worst=???ms
             kinds=sprite-empty=1 urls=<no-frame>@Image
```

（`.40` 之后 `open` 应该回到 **1 这样的小数字**——如果还是几百，说明我的判定还有问题，直接告诉我。）

**`worst` 就是结论：**

| 观察 | 结论 | 方向 |
|---|---|---|
| 几百 ms 且出现 `gap-close` | **正常的按需加载延迟**，会自愈 | 要治只能：预热 / 占位图 |
| 数秒 ~ 数十秒后 `gap-close` | **加载很慢但不是卡死** | 查 CDN 排队（`waiting for shared download`）/ 预热 |
| **永不 `gap-close`、`worst` 一直涨** | **GLoader 卡死了** | 查 FGUI `GLoader` 的加载回调与失败路径 |

这一条链路查到这里，**离定性只差这一个数字**。

---

# 22. 替换 JS 引擎：2.4.9 → 2.4.15，Debug/Release 分开（buildTag 2026-09-21.41）

## 22.1 换了什么

| | 之前 | 现在（Debug） | 现在（Release） |
|---|---|---|---|
| 引擎 | 2.4.9 min（被格式化成 115k 行） | **2.4.15 未压缩** | **2.4.15 min** |
| 体积 | 4.39 MB | 4.34 MB | **2.03 MB（省 2.4 MB）** |
| 类名 / 堆栈 | mangle（`Qi`/`zi`/`CCClass`） | **可读** | mangle |
| 来源 | 仓库内旧文件 | `src/vendor/cocos2d-2.4.15/cocos2d-js.js` | `…/cocos2d-js-min.js` |

**关键设计：目标路径恒为 `src/ios2-web-cocos2d.js`。**
`GameResourceSchemeHandler.localPathAliases` 把 `/cocos2d.js` 映射到它，
`ios2-web-index.html` 也写死了这个 URL ⇒ **换引擎只换"拷哪一个源文件"，别名与页面一个字都没动。**

## 22.2 落地清单（4 处）

1. **新增** `ios-cocos/cocos-project/src/vendor/cocos2d-2.4.15/`（`cocos2d-js.js` + `cocos2d-js-min.js`，共 6.1 MB）。
   放在 `vendor/` 子目录有两层好处：`copy-webruntime.sh` 的 `src/ios2-web-*.js` 自动纳入循环和
   `src/*.js` 未归类告警**都只看顶层**，不会误拷、也不会误告警。
2. **`Scripts/copy-webruntime.sh`**：新增引擎选择块（按 `${CONFIGURATION}`，可用 `IOS2_ENGINE=legacy` 回退）；
   并把"换引擎要重新核对什么"写进了脚本头部的同步点清单。
3. **`ios2-web-boot.js`**：① 视口指纹加 `engine=<cc.ENGINE_VERSION>`；
   ② 加固 `cc.Texture2D._nativeAsset` 取 accessor 的方式（见 22.4）。
4. `buildTag` → `2026-09-21.41`。

## 22.3 四条分支实测（脚本级，可直接复现）

```
CONFIGURATION=Debug    → src/vendor/cocos2d-2.4.15/cocos2d-js.js       4,336,942 B  engineVersion="2.4.15"
CONFIGURATION=Release  → src/vendor/cocos2d-2.4.15/cocos2d-js-min.js   2,034,380 B  cc.ENGINE_VERSION="2.4.15"
CONFIGURATION=Profile  → 同上（min）
IOS2_ENGINE=legacy     → src/ios2-web-cocos2d.js                       4,386,489 B  cc.ENGINE_VERSION = "2.4.9"
IOS2_ENGINE=bogus      → error: [Copy WebRuntime] IOS2_ENGINE 只认 legacy / 2415  ✅ 拒绝构建
```

Debug 全量构建通过，产物 `Contents/Resources/WebRuntime/src/ios2-web-cocos2d.js`
**已是 2.4.15**；旧引擎仍在仓库原位（回退门完好）。

## 22.4 顺手加固的两处（都是"静默失效"类，换引擎时最容易踩）

**(1) `_nativeAsset` 的 accessor 查找**

`installASTCTextureSupport` 原来直接 `getOwnPropertyDescriptor(cc.Texture2D.prototype, '_nativeAsset')`，
而 2.4.15 里它是从基类 **`override`** 过来的：

```js
_nativeAsset: {
  get: function get() { return this._image; },
  set: function set(data) { … init… },
  override: true
}
```

一旦定义位置挪一层，这个取值会拿到 `undefined` —— 而后面那句 `return` 是**静默**的：
**ASTC 解析/上传整条链失效、贴图全空、日志一条都没有。**
现在改成**沿原型链向上找真正定义 accessor 的那一层**，并且：

- 找不到 → `postWebGraphicsLog('texture-patch-missing', …)`（走 graphics 通道，一定能到原生）
- 装成功 → `postWebGraphicsLog('texture-patch-ready', …)`（带上引擎版本）

这条与前面几次的教训是同一条：**"装上了"和"没装上"都必须自己会说话。**

**(2) 视口指纹带引擎版本**

```
[instance] WebGL viewport: reason=settled engine=2.4.15 win=296x526 … fit=FIXED_WIDTH (22红血.bin)
```

换引擎之后"跑的是哪一个"必须有据可查，否则又会掉进"改了但没生效"那类坑。

## 22.5 换引擎**不能**解决当前那个问题（重申）

破阴的"画面不全"是 **FGUI `GLoader` 在资源到位前的空窗**（审计：`Container/GLoader/Image` 的
`Sprite` 为 `no-texture`）。而 `cc.Sprite._applySpriteFrame` 在 2.4.9 与 2.4.15 里**一字不差**
（已在 2.4.15 源码里逐行核对）⇒ **换了引擎，GLoader 也不会提前拿到图。**

**换引擎真正的收益**：Debug 类名/堆栈可读（`classifyRenderer` 不再需要 `instanceof` 兜底）、
Release 省 2.4 MB、以及 2.4.9→2.4.15 之间的官方修复。

## 22.6 ⚠️ 还没验证的（必须实跑，我无法在这里验）

| 项 | 风险 | 怎么看 |
|---|---|---|
| **资源序列化兼容** | 资产是 Creator 2.4.x 出的；2.4.15 的 `_parseExt` 已核对一致（候选 `"0_5@"` → `.png`/`.pvr`，`extnames[5]` 两版都是 `.pvr`）⇒ 选格式结果不变 | 进主界面看贴图是否正常 |
| **`jsb-adapter/game-defines.js`** | **2.4.9 的 adapter 配 2.4.15 引擎**，这是最可疑的一处 | 看启动是否报错 |
| **第三方脚本** | `xyzw-ls-4.5.5` 等可能依赖引擎内部实现 | 看脚本面板是否正常 |
| **`texture-patch-ready`** | 若没出现 = ASTC 管线没接管（贴图会全空） | `grep texture-patch` |

**启动后先看这两行，3 秒就能判断换得对不对：**

```bash
grep "texture-patch" <日志>        # ready = ASTC 管线接管成功；missing = 立刻回退
grep "WebGL viewport" <日志>       # engine=2.4.15
```

**回退**：构建时传 `IOS2_ENGINE=legacy`（或在 Xcode 的 Copy WebRuntime 阶段加这个环境变量）
⇒ 立刻回到 2.4.9，不需要改任何代码。

---

# 23. 引擎真正落库（buildTag 2026-09-21.42）——**修正 §22 的一个设计缺陷**

## 23.1 上一轮漏了一层

§22 只把 2.4.15 放进了 `vendor/`，**规范路径 `src/ios2-web-cocos2d.js` 仍是 2.4.9**（构建时才拷过去）。
这留下一个坑：**读那个文件的人会以为跑的就是它** —— 正是这个项目最忌讳的"路径与实效不一致"
（"改了但没生效"那一类，这次是反过来的"生效了但读不到"）。

## 23.2 该放哪：仓库本来就规定了

`src/ios2-web-cocos2d.js` 是**项目自带引擎槽位**，而且项目**本来就有导入机制**：

```sh
# ios-cocos/scripts/prepare_ios2.sh:24 / prepare_ios2_xcode.sh:13
WEB_ENGINE_SOURCE=${IOS2_WEB_ENGINE:-}
… cp "$WEB_ENGINE_SOURCE" "$WEB_ENGINE_TARGET"      # 目标就是 src/ios2-web-cocos2d.js
# 缺文件时脚本自己会提示：
#   set IOS2_WEB_ENGINE=/path/to/cocos2d-js-min.js once to import a replacement.
```

⇒ **"把 2.4.15 移植进项目"的正确形态，就是让这个槽位本身就是 2.4.15。**
（顺带确认：`prepare_ios2.sh` 只在 `src/` **不存在**时才从 source app 整目录拷，
注释写着 "never overwrite tracked ios2 changes as a side effect of a build" ⇒ **不会覆盖我们的引擎**。）

## 23.3 最终布局

```
ios-cocos/cocos-project/src/
  ios2-web-cocos2d.js                      4,336,942 B  ← 项目自带引擎槽位 = 2.4.15 未压缩
                                                          （与官方 cocos2d-js.js **逐字节一致**；Debug 用）
  vendor/cocos2d-2.4.15/cocos2d-js-min.js  2,034,380 B  ← Release / Profile
  vendor/cocos2d-2.4.9/ios2-web-cocos2d.js 4,386,489 B  ← IOS2_ENGINE=legacy 的回退门
  （vendor 合计 6.9 MB）
```

**规范路径上放的就是实际生效的那一份** —— 读文件即读引擎，不再有"看的是 A、跑的是 B"。

## 23.4 顺带查清：wx 兼容补丁已经是死代码

`prepare_ios2.sh` / `prepare_ios2_xcode.sh` 里都有一句：

```sh
perl -0pi -e 's/(console\.error\("This device does not support webgl"\),\s*)wx\)\)/$1typeof wx !== "undefined" \&\& wx))/g' "$WEB_ENGINE_TARGET"
```

**它在三份引擎上都匹配不上**（我逐个数过 `…webgl"), wx))` 的出现次数：**全为 0**）：

```
2.4.9（旧）  : (i || console.error("This device does not support webgl"), (this._gl = i), …    ← 没有 wx
2.4.15 未压缩: gl || console.error("This device does not support webgl"); this._gl = gl; …     ← 没有 wx
2.4.15 min   : i||console.error("…"),this._gl=i,…                                             ← 没有 wx
```

上游早就把 WeChat 依赖去掉了。⇒ **换引擎不必考虑这条补丁**（那两个脚本我没动，只在
`copy-webruntime.sh` 的注释里记了一笔，避免下次有人以为它还在起作用）。

## 23.5 三条分支实测 + 构建

```
Debug   → src/ios2-web-cocos2d.js                        4,336,942 B  engineVersion="2.4.15"
Release → src/vendor/cocos2d-2.4.15/cocos2d-js-min.js    2,034,380 B  cc.ENGINE_VERSION="2.4.15"
legacy  → src/vendor/cocos2d-2.4.9/ios2-web-cocos2d.js   4,386,489 B  cc.ENGINE_VERSION = "2.4.9"
```

Debug 全量构建通过；产物 `WebRuntime/src/ios2-web-cocos2d.js` = 4,336,942 B、`engineVersion="2.4.15"`，
且**与仓库规范路径逐字节一致**。

## 23.6 两件要注意的（我没动，但要记着）

1. **`prepare_ios2_xcode.sh` 每次 iOS 构建都会 `perl -0pi` 就地重写 `src/ios2-web-cocos2d.js`**
   —— 内容不变（补丁匹配不上），但 mtime 会变。这是既存行为。
2. **iOS 的 WebView 路径吃的是同一个文件**（`IOS2GameWebView.mm` 把 `cocos2d.js` → `ios2-web-cocos2d.js`）
   ⇒ 如果还编 iOS 的 WebView 版，换引擎会**一起**影响到它。
   （iOS 原生 JSB 路径用的是另一个文件 `src/cocos2d-jsb.07adf.js`，**不受影响也应该不受影响** ——
   你给的两份是 H5 引擎，**不能**拿去换 JSB 那份。）

## 23.7 待办

- **`vendor/` 里缺 Cocos 引擎的 LICENSE**。`vendor/` 已有 `FAIRYGUI-LICENSE.txt` 的先例，
  Cocos 引擎应该同样补一份（我没凭空造文本，留给你确认来源）。
- 实跑验证项见 §22.6：`grep texture-patch`（应见 `ready`）、`grep "WebGL viewport"`（应见 `engine=2.4.15`）、
  `jsb-adapter/game-defines.js` 的兼容、第三方脚本。

---

# 24. 「后面两个 bin 进不到玩具页面」（日志**尾部片段**，6 实例）

> 用户这次给的是**尾部片段**（137 行，6 个实例：超级03/04/13、霸道02/03/04），
> **玩具页面打开的那一刻不在片段里**。所以下面是片段能确定的部分 + 要补的东西。

## 24.1 片段里最刺眼的一行：**超级04 被"关闭参与同步"**

```
94: [sync] 参与同步 关闭 超级04（分组=小 参与者=5 主控=无）
95: [sync] page 超级04 → {"agent":2,"capturing":false,"ripple":true,"replayed":6304,"misses":0,…}
```

紧接着的同步点击目标列表**从 5 个掉到 4 个，且不含超级04**：

```
之前: [sync] publish 超级03 mousedown → 5 目标 [超级04, 超级13, 霸道02, 霸道03, 霸道04]
之后: [sync] publish 超级03 mousedown → 4 目标 [超级13, 霸道02, 霸道03, 霸道04]
```

⇒ **它不再接收任何同步点击，自然"进不去"任何被同步打开的页面。**

### 这个开关**只有界面入口**（我把写入路径全查了）

| 入口 | 位置 |
|---|---|
| 单个窗口的 **🔗 参与同步**（矩阵卡片顶栏：序号 / 昵称 / 👑主控 / **🔗参与同步** / 重载 / 关闭） | `MatrixStageView.swift:655` → `sync.toggleReceiver(account.id)` |
| **一键**开启/关闭全部同步 | `MatrixStageView.swift:364/401` → `disableAllSync()` / `enableAllLiveInstances()` |
| **按组**关闭同步 | `MatrixStageView.swift:414` → `disableGroup(group.id)` |
| 侧栏「开启/关闭组内同步」 | `AccountSidebarView.swift:226/228` |

`setReceiver(_:enabled:)`（`InputSyncController.swift:598`）**没有任何自动调用路径** ——
不是实例重载、不是断线、不是页面导航会触发的。**⇒ 先看那两个窗口的 🔗 是不是被关掉了。**

> 另外 `分组=小` **不是"大小档"**，是**账号分组名**（`groupName(for:)`）——
> 这批账号在名为「小」的分组里，该组参与者 5 人（关掉一个后）。
> 路由是**按分组隔离**的：**不在同一分组的窗口，本来就不会跟着走。**

## 24.2 片段里的分叉实证（与 §16.2 / §19.2 同一条线）

同一次点击之后，各窗口请求的 bundle：

```
ui_weapon          → 超级03 / 超级04 / 超级13 / 霸道02 / 霸道03 / 霸道04   （6 个都请求了）
xiaobing4_feidao   → 只有 超级13
zhangfei           → 只有 超级13
dianwei            → 只有 超级03（8 次网络下载，by=超级03.bin）
```

**而且 `dianwei` 那 8 次下载里一条 `waiting for shared download` 都没有**
⇒ **只有超级03 一个窗口需要它**（其余窗口根本没请求）。

⇒ **又一份"同一次点击在不同窗口落到不同界面"的实录**：大家在同一个界面上，
但点下去之后有的进 A、有的进 B，于是各窗口按需加载的 bundle 完全不同。
**"有的进得去、有的进不去"，就是这件事的表现。**

## 24.3 片段里还有两次主循环真重建

```
76/77 : 超级03 焦点=off → ok-restart:15    +  超级04 焦点=on → ok-restart:30
118/119: 超级04 焦点=off → ok-restart:15    +  超级03 焦点=on → ok-restart:30
```

焦点在 **超级03 ↔ 超级04** 之间来回两次，**两个实例各被拆建一次**
（`pause()` + 等 ≥120ms + `_setAnimFrame` + `resume()`），而且正落在密集点击期间。

## 24.4 要补的东西（片段不够）

1. **具体是哪两个 bin** —— 片段里只看到 `超级04` 退出了同步。
2. **那两个窗口的卡片顶栏 🔗 是否亮着**、**是否与其它窗口同分组**（这是最省事的两个检查）。
3. **完整日志**（现在是尾部 137 行，玩具页面打开那一刻不在里面）。
   最好带上 `参与同步` 全量行 —— 它能直接说明"谁在组里、什么时候掉的"。

## 24.5 建议补的观测（能一次回答"这次点击各窗口分别进了哪"）

现在的 `[scheme] bundle requests in 60s` 是**时间窗**汇总，无法对齐到**单次点击**。
建议加一条**点击维度的差量**：每次 `[sync] publish` 之后 2 秒，把各窗口在这 2 秒内
**新请求的 bundle** 按账号汇总一条，例如

```
[sync] click#412 超级03 → 超级04:ui_weapon 超级13:ui_weapon,xiaobing4_feidao,zhangfei 霸道02:ui_weapon …
```

这样"5 个窗口进了 A、2 个进了 B"会**直接写在日志里**，不必再靠时间窗猜。
（代价：每次点击多一条日志；实现需要在 publish 处触发一次各 handler 的差量快照。）
